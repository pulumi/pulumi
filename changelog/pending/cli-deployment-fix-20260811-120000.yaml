component: cli/deployment
kind: fix
body: "Allow `pulumi deployment settings edit` to configure every version control provider and the trigger, git authentication, deployment role, caching and template settings, stop it overwriting or clearing settings it was not asked to change, and stop `get` failing on an unconfigured stack"
time: 2026-08-11T12:00:00+00:00
