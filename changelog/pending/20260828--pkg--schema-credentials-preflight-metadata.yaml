component: pkg
kind: improvement
body: Add the `configurationDocsUrl` and `validateCredentialsOnNew` package schema fields so providers can opt into the `pulumi new` credentials check and link to their configuration docs
time: 2026-08-28T00:00:00Z
custom:
    PR: "24456"
