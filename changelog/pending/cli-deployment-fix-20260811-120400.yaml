component: cli/deployment
kind: fix
body: "`pulumi deployment settings edit` no longer builds patches that clear settings it was not asked to change, `--branch` and `--commit` now replace one another, and a stack configured against a repository URL can move onto a version control integration"
time: 2026-08-11T12:04:00+00:00
