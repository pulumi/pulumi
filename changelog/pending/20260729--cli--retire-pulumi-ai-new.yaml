component: cli
kind: chore
body: Retire the Pulumi AI mode of `pulumi new` (interactive choice and `--ai`/`--language`
    flags). The backing service has been shut down; use templates or Pulumi Neo instead.
