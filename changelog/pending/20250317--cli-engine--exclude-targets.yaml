changes:
- type: feat
  scope: cli/engine
  description: Allow targets to be excluded with the `--exclude` flag. `--exclude-dependents` can also be used to exclude all children of an excluded target as well
