changes:
- type: feat
  scope: engine
  description: |
    Reject invalid property paths from programs

    BREAKING: Programs that send invalid property paths to the engine will begin
    to error. Previously these property paths were ignored.

    Loosely parsed property paths from state will continue to be accepted as
    they were before.
