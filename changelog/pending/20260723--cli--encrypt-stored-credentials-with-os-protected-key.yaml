component: cli
kind: feat
body: Add opt-in encryption of stored credentials via `PULUMI_CREDENTIAL_STORE`, protecting
    the key with the macOS Keychain (per-app scoped for signed builds), Windows Credential
    Manager, or Linux Secret Service, TPM-hardened where available, with on-demand migration
    of existing plaintext credentials
custom:
    PR: "24076"
