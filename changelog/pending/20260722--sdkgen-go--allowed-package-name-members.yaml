component: sdkgen/go
kind: fix
body: Generate and honour a package reference for members whose token names a package
    declared in allowedPackageNames, so an explicit provider is no longer dropped
custom:
    PR: "24010"
