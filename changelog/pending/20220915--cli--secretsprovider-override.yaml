changes:
- type: feat
  scope: cli
  description: >
    Allow per-execution override of the secrets provider via the
    `PULUMI_CLOUD_SECRET_OVERRIDE` environment variable. This allows a
    temporary replacement without updating the stack config, such a during CI.
