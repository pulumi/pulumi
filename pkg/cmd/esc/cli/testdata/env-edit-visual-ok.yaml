run: |
  esc env edit test
  esc env get test
process:
  environ:
    VISUAL: my-editor
  commands:
    my-editor: |
      echo -e "values:\n  foo: baz\n" >$1
environments:
  test-user/test:
    values:
      foo: bar
stdout: |
  Environment updated.
  {
    "foo": "baz"
  }
