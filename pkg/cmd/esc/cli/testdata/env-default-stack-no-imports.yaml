run: esc env default
error: exit status 1
process:
  fs:
    Pulumi.dev.yaml: |
      config:
        my-project:size: t3.micro
    Pulumi.yaml: |
      name: my-project
      runtime: nodejs
pulumi:
  selected-stack: dev
environments:
  test-user/default/base:
    values:
      foo: bar

---
> esc env default

---
> esc env default
Error: no default environment
