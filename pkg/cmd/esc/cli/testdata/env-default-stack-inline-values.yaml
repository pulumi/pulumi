run: esc env default
process:
  fs:
    Pulumi.dev.yaml: |
      environment:
        imports:
          - default/base
        values:
          size: t3.micro
    Pulumi.yaml: |
      name: my-project
      runtime: nodejs
pulumi:
  selected-stack: dev
environments:
  test-user/default/base:
    values:
      foo: bar

---
> esc env default
environment:
  organization: test-user
  imports:
    - default/base
source: stack dev

---
> esc env default
