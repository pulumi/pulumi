run: esc env init dup
error: exit status 1
environments:
  test-user/default/dup: arbitrary
stdout: |
  > esc env init dup
stderr: |
  > esc env init dup
  Error: creating environment: already exists
