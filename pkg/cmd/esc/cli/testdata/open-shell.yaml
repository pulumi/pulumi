run: esc open test --format shell
environments:
  test-user/test:
    imports:
      - test-2
    values:
      foo: bar
      environmentVariables:
        FOO: ${foo}
  test-user/test-2:
    values:
      foo: baz
      hello: world
      environmentVariables:
        HELLO: ${hello}
stdout: |
  > esc open test --format shell
  export FOO="bar"
  export HELLO="world"
