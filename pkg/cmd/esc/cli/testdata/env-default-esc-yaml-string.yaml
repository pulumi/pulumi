run: esc env default
process:
  fs:
    .esc.yaml: |
      environment: test-user/default/test
environments:
  test-user/default/test:
    values:
      foo: bar

---
> esc env default
environment: test-user/default/test
source: .esc.yaml

---
> esc env default
