name: provider
runtime: yaml
description: A YAML program which uses a provider

resources:
  ls:
    type: command:local:Command
    properties:
      create: echo hello
      dir: ../

plugins:
  languages:
    - name: yaml
      path: bin
  providers:
    - name: command
      path: bin