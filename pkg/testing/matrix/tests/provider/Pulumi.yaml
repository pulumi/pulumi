name: provider
runtime: yaml
description: A YAML program which uses a provider

resources:
  ls:
    type: command:local:Command
    properties:
      create: echo hello
      dir: ../

plugins:
  languages:
    - name: yaml
      path: /Users/hliuson/go/src/github.com/pulumi/pulumi-yaml/bin
  providers:
    - name: command
      path: bin