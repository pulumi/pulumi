name: demo1-example
description: A simple Express voting app with a MongoDB backend, ready for production use.
services:
    private:
        - votes: mongodb/mongodb
    public:
        - app:
            type: mu/app
            arguments:
                db: @votes

