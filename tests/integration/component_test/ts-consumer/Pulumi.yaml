name: ts-consumer
description: A minimal TypeScript Pulumi program
packages:
  my-component: ../python-component
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
