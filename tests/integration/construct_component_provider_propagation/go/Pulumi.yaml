name: construct_component_provider_propagation_go
description: A program that constructs remote component resources with first class provider.
runtime: go
plugins:
  providers:
    - name: testprovider
      path: ../../../testprovider
