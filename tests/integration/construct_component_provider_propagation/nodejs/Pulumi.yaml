name: construct_component_provider_propagation_nodejs
description: A program that constructs remote component resources with first class provider.
runtime: nodejs
plugins:
  providers:
    - name: testprovider
      path: ../../../testprovider
