[project]
name = "some-package"
version = "0.1.0"
description = "A Python package"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["uv_build==0.8.24"]
build-backend = "uv_build"
