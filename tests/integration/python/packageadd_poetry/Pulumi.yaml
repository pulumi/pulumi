name: packageadd_poetry
description: A minimal Google Cloud Python Pulumi program
runtime:
  name: python
  options:
    toolchain: poetry
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
