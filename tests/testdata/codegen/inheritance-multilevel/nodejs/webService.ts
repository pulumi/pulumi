// *** WARNING: this file was generated by test. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

import {Service, ServiceArgs} from "./index";

/**
 * A leaf component that extends Service.
 */
export class WebService extends Service {
    /** @internal */
    public static readonly __pulumiType: string = 'example:index:WebService';

    /** @internal */
    public static readonly __pulumiBaseTypes: string[] = ['example:index:Service', 'example:index:Resource'];

    /**
     * Returns true if the given object is an instance of WebService (or a subclass of it).  This is
     * designed to work even when multiple copies of the Pulumi SDK have been loaded into the
     * same process.
     */
    public static isInstance(obj: any): obj is WebService {
        if (obj === undefined || obj === null) {
            return false;
        }
        if (obj instanceof WebService) {
            return true;
        }
        if (obj['__pulumiType'] === WebService.__pulumiType) {
            return true;
        }
        const baseTypes = obj?.constructor?.['__pulumiBaseTypes'];
        return Array.isArray(baseTypes) && baseTypes.indexOf(WebService.__pulumiType) !== -1;
    }

    declare public readonly replicas: pulumi.Output<number>;

    /**
     * Create a WebService resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: WebServiceArgs, opts?: pulumi.ComponentResourceOptions, __chain?: any /* internal */) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (__chain === undefined) {
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            if (args?.port === undefined && !opts.urn) {
                throw new Error("Missing required property 'port'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["port"] = args?.port;
            resourceInputs["replicas"] = args?.replicas;
            resourceInputs["id"] = undefined /*out*/;
            resourceInputs["url"] = undefined /*out*/;
            opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
            if (new.target !== WebService) {
                super(name, args, opts, { mode: "userSubclass", type: (new.target as any).__pulumiType, inputs: resourceInputs });
                void pulumi.runtime.constructBaseResource(this, "example:index:WebService", args ?? {}, { version: utilities.getVersion() }, ["id", "name", "port", "replicas", "url"]);
                return;
            }
        }
        super(name, args, opts, __chain ?? { mode: "generated", type: WebService.__pulumiType, inputs: resourceInputs });
    }
}

/**
 * The set of arguments for constructing a WebService resource.
 */
export interface WebServiceArgs extends ServiceArgs {
    replicas?: pulumi.Input<number | undefined>;
}
