load("@rules_go//go:def.bzl", "go_test")

go_test(
    name = "policy_new_test",
    srcs = ["policy_new_test.go"],
    data = ["//pkg/cmd/pulumi"],
    tags = [
        "integration",
        "manual",
    ],
    timeout = "long",
    deps = ["//sdk/go/common/testing"],
)
