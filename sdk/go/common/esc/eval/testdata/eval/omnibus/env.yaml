imports:
  - a
values:
  join:
    fn::join: [ ",", "${strings}" ]
  open:
    fn::open::test:
      baz: qux
  secret:
    fn::secret:
      hunter2
  toBase64:
    fn::toBase64: ${join}
  toJSON:
    fn::toJSON: ${open}
  toString:
    fn::toString: ${open}
  open2:
    fn::open::test: ${open}
  interp: hello, ${toString}
  access: ${open["baz"]}
