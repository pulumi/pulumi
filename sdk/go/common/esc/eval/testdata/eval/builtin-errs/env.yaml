values:
  number: 42
  builtins:
    - { fn::open::test: "${missing}" }
    - { fn::join: [",", "${missing}" ] }
    - { fn::join: [",", [ "${number}" ] ] }
    - { fn::toBase64: "${missing}" }
    - { fn::toBase64: "${number}" }
    - { fn::toJSON: "${missing}" }
    - { fn::toString: "${missing}" }
