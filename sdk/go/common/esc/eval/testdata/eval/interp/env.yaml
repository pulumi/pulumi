values:
  a:
    p: ${a.q}
    q: foo
    s: { fn::secret: [ shhhh ] }
    t: { fn::secret: { password: hunter2 } }
  b:
    p: ${a.p}
  c:
    a:
      - ${b.p}
      - hello, ${c.b}
      - ${c.a[0]} bar
    b: world!
    s:
      - ${a.s[0]}
      - ${a.t.password}
