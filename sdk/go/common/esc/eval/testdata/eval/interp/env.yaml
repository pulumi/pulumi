values:
  a:
    p: ${a.q}
    q: foo
    s: { fn::secret: [ shhhh ] }
    t: { fn::secret: { password: hunter2 } }
    u: { "\"baz": merp }
  b:
    p: ${a.p}
  c:
    a:
      - ${b.p}
      - hello, ${c.b}
      - ${c.a[0]} bar
    b: world!
    s:
      - ${a.s[0]}
      - ${a.t.password}
      - ${a.u["\"baz"]}
      - ${["34"]}
      - ${["35"].okay}
  "34": this should be okay
  "35":
    okay: also okay
