[project]
  name = "pulumi_parameterized"
  dependencies = ["parver>=0.2.1", "pulumi>=3.0.0,<4.0.0", "semver>=2.8.1"]
  readme = "README.md"
  requires-python = ">=3.8"
  version = "1.3.7"

[build-system]
  requires = ["setuptools>=61.0"]
  build-backend = "setuptools.build_meta"

[tool]
  [tool.setuptools]
    [tool.setuptools.package-data]
      pulumi_parameterized = ["py.typed", "pulumi-plugin.json"]
