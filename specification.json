{
  "type": "menu",
  "executable": false,
  "flags": {
    "color": {
      "name": "color",
      "type": "string",
      "description": "Colorize output. Choices are: always, never, raw, auto"
    },
    "cwd": {
      "name": "cwd",
      "type": "string",
      "description": "Run pulumi as if it had been started in another directory"
    },
    "disable-integrity-checking": {
      "name": "disable-integrity-checking",
      "type": "boolean",
      "description": "Disable integrity checking of checkpoint files"
    },
    "emoji": {
      "name": "emoji",
      "type": "boolean",
      "description": "Enable emojis in the output"
    },
    "fully-qualify-stack-names": {
      "name": "fully-qualify-stack-names",
      "type": "boolean",
      "description": "Show fully-qualified stack names"
    },
    "logflow": {
      "name": "logflow",
      "type": "boolean",
      "description": "Flow log settings to child processes (like plugins)"
    },
    "logtostderr": {
      "name": "logtostderr",
      "type": "boolean",
      "description": "Log to stderr instead of to files"
    },
    "memprofilerate": {
      "name": "memprofilerate",
      "type": "int",
      "description": "Enable more precise (and expensive) memory allocation profiles by setting runtime.MemProfileRate"
    },
    "non-interactive": {
      "name": "non-interactive",
      "type": "boolean",
      "description": "Disable interactive mode for all commands"
    },
    "profiling": {
      "name": "profiling",
      "type": "string",
      "description": "Emit CPU and memory profiles and an execution trace to '[filename].[pid].{cpu,mem,trace}', respectively"
    },
    "tracing": {
      "name": "tracing",
      "type": "string",
      "description": "Emit tracing to the specified endpoint. Use the `file:` scheme to write tracing data to a local file"
    },
    "tracing-header": {
      "name": "tracing-header",
      "type": "string",
      "description": "Include the tracing header with the given contents."
    },
    "verbose": {
      "name": "verbose",
      "type": "int",
      "description": "Enable verbose logging (e.g., v=3); anything \u003e3 is very verbose"
    }
  },
  "commands": {
    "about": {
      "type": "menu",
      "executable": true,
      "flags": {
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Emit output as JSON"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to get info on. Defaults to the current stack"
        },
        "transitive": {
          "name": "transitive",
          "type": "boolean",
          "description": "Include transitive dependencies"
        }
      },
      "commands": {
        "env": {
          "type": "command",
          "arguments": {
            "arguments": []
          },
          "description": "An overview of the environmental variables used by pulumi"
        }
      }
    },
    "ai": {
      "type": "menu",
      "executable": true,
      "commands": {
        "web": {
          "type": "command",
          "flags": {
            "language": {
              "name": "language",
              "type": "string",
              "description": "Language to use for the prompt - this defaults to TypeScript. [TypeScript, Python, Go, C#, Java, YAML]"
            },
            "no-auto-submit": {
              "name": "no-auto-submit",
              "type": "boolean",
              "description": "Opt-out of automatically submitting the prompt to Pulumi AI"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "prompt"
              }
            ]
          },
          "description": "Opens Pulumi AI in your local browser\n\nThis command opens the Pulumi AI web app in your local default browser.\nIt can be further initialized by providing a prompt to pre-fill in the app,\nwith the default behavior then automatically submitting that prompt to Pulumi AI.\n\nIf you do not want to submit the prompt to Pulumi AI, you can opt-out of this\nby passing the --no-auto-submit flag.\n\nExample:\n  pulumi ai web \"Create an S3 bucket in Python\""
        }
      }
    },
    "cancel": {
      "type": "command",
      "flags": {
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        },
        "yes": {
          "name": "yes",
          "type": "boolean",
          "description": "Skip confirmation prompts, and proceed with cancellation anyway"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "stack-name"
          }
        ]
      },
      "description": "Cancel a stack's currently running update, if any.\n\nThis command cancels the update currently being applied to a stack if any exists.\nNote that this operation is _very dangerous_, and may leave the stack in an\ninconsistent state if a resource operation was pending when the update was canceled.\n\nAfter this command completes successfully, the stack will be ready for further\nupdates."
    },
    "config": {
      "type": "menu",
      "executable": true,
      "flags": {
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Emit output as JSON"
        },
        "open": {
          "name": "open",
          "type": "boolean",
          "description": "Open and resolve any environments listed in the stack configuration. Defaults to true if --show-secrets is set, false otherwise"
        },
        "show-secrets": {
          "name": "show-secrets",
          "type": "boolean",
          "description": "Show secret values when listing config instead of displaying blinded values"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        }
      },
      "commands": {
        "cp": {
          "type": "command",
          "flags": {
            "dest": {
              "name": "dest",
              "type": "string",
              "description": "The name of the new stack to copy the config to"
            },
            "path": {
              "name": "path",
              "type": "boolean",
              "description": "The key contains a path to a property in a map or list to set"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "key"
              }
            ]
          },
          "description": "Copies the config from the current stack to the destination stack. If `key` is omitted,\nthen all of the config from the current stack will be copied to the destination stack."
        },
        "env": {
          "type": "menu",
          "executable": false,
          "commands": {
            "add": {
              "type": "command",
              "flags": {
                "show-secrets": {
                  "name": "show-secrets",
                  "type": "boolean",
                  "description": "Show secret values in plaintext instead of ciphertext"
                },
                "yes": {
                  "name": "yes",
                  "type": "boolean",
                  "description": "True to save changes without prompting"
                }
              },
              "arguments": {
                "arguments": [
                  {
                    "name": "environment-name"
                  }
                ],
                "requiredArguments": 1,
                "variadic": true
              },
              "description": "Adds environments to the end of a stack's import list. Imported environments are merged in order\nper the ESC merge rules. The list of stacks behaves as if it were the import list in an anonymous\nenvironment."
            },
            "init": {
              "type": "command",
              "flags": {
                "env": {
                  "name": "env",
                  "type": "string",
                  "description": "The name of the environment to create. Defaults to \"\u003cproject name\u003e/\u003cstack name\u003e\""
                },
                "keep-config": {
                  "name": "keep-config",
                  "type": "boolean",
                  "description": "Do not remove configuration values from the stack after creating the environment"
                },
                "show-secrets": {
                  "name": "show-secrets",
                  "type": "boolean",
                  "description": "Show secret values in plaintext instead of ciphertext"
                },
                "yes": {
                  "name": "yes",
                  "type": "boolean",
                  "description": "True to save the created environment without prompting"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Creates an environment for a specific stack based on the stack's configuration values,\nthen replaces the stack's configuration values with a reference to that environment.\nThe environment will be created in the same organization as the stack."
            },
            "ls": {
              "type": "command",
              "flags": {
                "json": {
                  "name": "json",
                  "type": "boolean",
                  "description": "Emit output as JSON"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Lists the environments imported into a stack's configuration."
            },
            "rm": {
              "type": "command",
              "flags": {
                "show-secrets": {
                  "name": "show-secrets",
                  "type": "boolean",
                  "description": "Show secret values in plaintext instead of ciphertext"
                },
                "yes": {
                  "name": "yes",
                  "type": "boolean",
                  "description": "True to save changes without prompting"
                }
              },
              "arguments": {
                "arguments": [
                  {
                    "name": "environment-name"
                  }
                ],
                "requiredArguments": 1
              },
              "description": "Removes an environment from a stack's import list."
            }
          }
        },
        "get": {
          "type": "command",
          "flags": {
            "json": {
              "name": "json",
              "type": "boolean",
              "description": "Emit output as JSON"
            },
            "open": {
              "name": "open",
              "type": "boolean",
              "description": "Open and resolve any environments listed in the stack configuration"
            },
            "path": {
              "name": "path",
              "type": "boolean",
              "description": "The key contains a path to a property in a map or list to get"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "key"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Get a single configuration value.\n\nThe `--path` flag can be used to get a value inside a map or list:\n\n  - `pulumi config get --path outer.inner` will get the value of the `inner` key, if the value of `outer` is a map `inner: value`.\n  - `pulumi config get --path 'names[0]'` will get the value of the first item, if the value of `names` is a list."
        },
        "refresh": {
          "type": "command",
          "flags": {
            "force": {
              "name": "force",
              "type": "boolean",
              "description": "Overwrite configuration file, if it exists, without creating a backup"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "Update the local configuration based on the most recent deployment of the stack"
        },
        "rm": {
          "type": "command",
          "flags": {
            "path": {
              "name": "path",
              "type": "boolean",
              "description": "The key contains a path to a property in a map or list to remove"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "key"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Remove configuration value.\n\nThe `--path` flag can be used to remove a value inside a map or list:\n\n  - `pulumi config rm --path outer.inner` will remove the `inner` key, if the value of `outer` is a map `inner: value`.\n  - `pulumi config rm --path 'names[0]'` will remove the first item, if the value of `names` is a list."
        },
        "rm-all": {
          "type": "command",
          "flags": {
            "path": {
              "name": "path",
              "type": "boolean",
              "description": "Parse the keys as paths in a map or list rather than raw strings"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "key"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "Remove multiple configuration values.\n\nThe `--path` flag indicates that keys should be parsed within maps or lists:\n\n  - `pulumi config rm-all --path  outer.inner 'foo[0]' key1` will remove the \n    `inner` key of the `outer` map, the first key of the `foo` list and `key1`.\n  - `pulumi config rm-all outer.inner 'foo[0]' key1` will remove the literal    `outer.inner`, `foo[0]` and `key1` keys"
        },
        "set": {
          "type": "command",
          "flags": {
            "path": {
              "name": "path",
              "type": "boolean",
              "description": "The key contains a path to a property in a map or list to set"
            },
            "plaintext": {
              "name": "plaintext",
              "type": "boolean",
              "description": "Save the value as plaintext (unencrypted)"
            },
            "secret": {
              "name": "secret",
              "type": "boolean",
              "description": "Encrypt the value instead of storing it in plaintext"
            },
            "type": {
              "name": "type",
              "type": "string",
              "description": "Save the value as the given type.  Allowed values are string, bool, int, and float"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "key"
              },
              {
                "name": "value"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Configuration values can be accessed when a stack is being deployed and used to configure behavior. \nIf a value is not present on the command line, pulumi will prompt for the value. Multi-line values\nmay be set by piping a file to standard in.\n\nThe `--path` flag can be used to set a value inside a map or list:\n\n  - `pulumi config set --path 'names[0]' a` will set the value to a list with the first item `a`.\n  - `pulumi config set --path parent.nested value` will set the value of `parent` to a map `nested: value`.\n  - `pulumi config set --path '[\"parent.name\"][\"nested.name\"]' value` will set the value of \n    `parent.name` to a map `nested.name: value`.\n\nWhen setting the config for a path, \"true\" and \"false\" are treated as boolean values, and\nintegers are treated as numbers. All other values are treated as strings.  Top level entries\nare always treated as strings."
        },
        "set-all": {
          "type": "command",
          "flags": {
            "json": {
              "name": "json",
              "type": "string",
              "description": "Read values from a JSON string in the format produced by 'pulumi config --json'"
            },
            "path": {
              "name": "path",
              "type": "boolean",
              "description": "Parse the keys as paths in a map or list rather than raw strings"
            },
            "plaintext": {
              "name": "plaintext",
              "type": "string",
              "description": "Marks a value as plaintext (unencrypted)",
              "repeatable": true
            },
            "secret": {
              "name": "secret",
              "type": "string",
              "description": "Marks a value as secret to be encrypted",
              "repeatable": true
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "pulumi set-all allows you to set multiple configuration values in one command.\n\nEach key-value pair must be preceded by either the `--secret` or the `--plaintext` flag to denote whether \nit should be encrypted:\n\n  - `pulumi config set-all --secret key1=value1 --plaintext key2=value --secret key3=value3`\n\nThe `--path` flag can be used to set values inside a map or list:\n\n  - `pulumi config set-all --path --plaintext \"names[0]\"=a --plaintext \"names[1]\"=b` \n    will set the value to a list with the first item `a` and second item `b`.\n  - `pulumi config set-all --path --plaintext parent.nested=value --plaintext parent.other=value2` \n    will set the value of `parent` to a map `{nested: value, other: value2}`.\n  - `pulumi config set-all --path --plaintext '[\"parent.name\"].[\"nested.name\"]'=value` will set the \n    value of `parent.name` to a map `nested.name: value`.\n\nThe `--json` flag can be used to pass a JSON string from which values should be read.\nThe JSON string should follow the same format as that produced by `pulumi config --json`. If the\n`--json` option is passed, the `--plaintext`, `--secret` and `--path` flags must not be used."
        }
      }
    },
    "console": {
      "type": "command",
      "flags": {
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to view"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "Opens the current stack in the Pulumi Console"
    },
    "convert": {
      "type": "command",
      "flags": {
        "from": {
          "name": "from",
          "type": "string",
          "description": "Which converter plugin to use to read the source program"
        },
        "generate-only": {
          "name": "generate-only",
          "type": "boolean",
          "description": "Generate the converted program(s) only; do not install dependencies"
        },
        "language": {
          "name": "language",
          "type": "string",
          "description": "Which language plugin to use to generate the Pulumi project"
        },
        "mappings": {
          "name": "mappings",
          "type": "string",
          "description": "Any mapping files to use in the conversion",
          "repeatable": true
        },
        "name": {
          "name": "name",
          "type": "string",
          "description": "The name to use for the converted project; defaults to the directory of the source project"
        },
        "out": {
          "name": "out",
          "type": "string",
          "description": "The output directory to write the converted project to"
        },
        "strict": {
          "name": "strict",
          "type": "boolean",
          "description": "Fail the conversion on errors such as missing variables"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "arg"
          }
        ],
        "variadic": true
      },
      "description": "Convert Pulumi programs from a supported source program into other supported languages.\n\nThe source program to convert will default to the current working directory.\n\nValid source languages: yaml, terraform, bicep, arm, kubernetes\n\nValid target languages: typescript, python, csharp, go, java, yaml\nExample command usage:\n    pulumi convert --from yaml --language java --out . \n\n\nNote that certain target languages may require additional arguments to be passed to this command."
    },
    "convert-trace": {
      "type": "command",
      "flags": {
        "granularity": {
          "name": "granularity",
          "type": "string",
          "description": "the sample granularity"
        },
        "ignore-log-spans": {
          "name": "ignore-log-spans",
          "type": "boolean",
          "description": "true to ignore log spans"
        },
        "otel": {
          "name": "otel",
          "type": "boolean",
          "description": "true to export to OpenTelemetry"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "trace-file"
          }
        ],
        "requiredArguments": 1
      },
      "description": "Convert a trace from the Pulumi CLI to Google's pprof format.\n\nThis command is used to convert execution traces collected by a prior\ninvocation of the Pulumi CLI from their native format to Google's\npprof format. The converted trace is written to stdout, and can be\ninspected using `go tool pprof`."
    },
    "deployment": {
      "type": "menu",
      "executable": true,
      "flags": {
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Override the file name where the deployment settings are specified. Default is Pulumi.[stack].deploy.yaml"
        }
      },
      "commands": {
        "run": {
          "type": "command",
          "flags": {
            "agent-pool-id": {
              "name": "agent-pool-id",
              "type": "string",
              "description": "The agent pool to use to run the deployment job. When empty, the Pulumi Cloud shared queue will be used."
            },
            "env": {
              "name": "env",
              "type": "string",
              "description": "Environment variables to use in the remote operation of the form NAME=value (e.g. `--env FOO=bar`)",
              "repeatable": true
            },
            "env-secret": {
              "name": "env-secret",
              "type": "string",
              "description": "Environment variables with secret values to use in the remote operation of the form NAME=secretvalue (e.g. `--env FOO=secret`)",
              "repeatable": true
            },
            "executor-image": {
              "name": "executor-image",
              "type": "string",
              "description": "The Docker image to use for the executor"
            },
            "executor-image-password": {
              "name": "executor-image-password",
              "type": "string",
              "description": "The password for the credentials with access to the Docker image to use for the executor"
            },
            "executor-image-username": {
              "name": "executor-image-username",
              "type": "string",
              "description": "The username for the credentials with access to the Docker image to use for the executor"
            },
            "git-auth-access-token": {
              "name": "git-auth-access-token",
              "type": "string",
              "description": "Git personal access token"
            },
            "git-auth-password": {
              "name": "git-auth-password",
              "type": "string",
              "description": "Git password; for use with username or with an SSH private key"
            },
            "git-auth-ssh-private-key": {
              "name": "git-auth-ssh-private-key",
              "type": "string",
              "description": "Git SSH private key; use --git-auth-password for the password, if needed"
            },
            "git-auth-ssh-private-key-path": {
              "name": "git-auth-ssh-private-key-path",
              "type": "string",
              "description": "Git SSH private key path; use --git-auth-password for the password, if needed"
            },
            "git-auth-username": {
              "name": "git-auth-username",
              "type": "string",
              "description": "Git username"
            },
            "git-branch": {
              "name": "git-branch",
              "type": "string",
              "description": "Git branch to deploy; this is mutually exclusive with --git-commit; either value needs to be specified"
            },
            "git-commit": {
              "name": "git-commit",
              "type": "string",
              "description": "Git commit hash of the commit to deploy (if used, HEAD will be in detached mode); this is mutually exclusive with --git-branch; either value needs to be specified"
            },
            "git-repo-dir": {
              "name": "git-repo-dir",
              "type": "string",
              "description": "The directory to work from in the project's source repository where Pulumi.yaml is located; used when Pulumi.yaml is not in the project source root"
            },
            "inherit-settings": {
              "name": "inherit-settings",
              "type": "boolean",
              "description": "Inherit deployment settings from the current stack"
            },
            "pre-run-command": {
              "name": "pre-run-command",
              "type": "string",
              "description": "Commands to run before the remote operation",
              "repeatable": true
            },
            "skip-install-dependencies": {
              "name": "skip-install-dependencies",
              "type": "boolean",
              "description": "Whether to skip the default dependency installation step"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "suppress-permalink": {
              "name": "suppress-permalink",
              "type": "boolean",
              "description": "Suppress display of the state permalink"
            },
            "suppress-stream-logs": {
              "name": "suppress-stream-logs",
              "type": "boolean",
              "description": "Suppress log streaming of the deployment job"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "operation"
              },
              {
                "name": "url"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Launch a deployment job on Pulumi Cloud\n\nThis command queues a new deployment job for any supported operation of type \nupdate, preview, destroy, refresh, detect-drift or remediate-drift."
        },
        "settings": {
          "type": "menu",
          "executable": true,
          "commands": {
            "configure": {
              "type": "command",
              "flags": {
                "git-auth-ssh-private-key": {
                  "name": "git-auth-ssh-private-key",
                  "type": "string",
                  "description": "Git SSH private key"
                },
                "git-auth-ssh-private-key-path": {
                  "name": "git-auth-ssh-private-key-path",
                  "type": "string",
                  "description": "Private key path"
                },
                "stack": {
                  "name": "stack",
                  "type": "string",
                  "description": "The name of the stack to operate on. Defaults to the current stack"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Updates stack's deployment settings secrets"
            },
            "destroy": {
              "type": "command",
              "flags": {
                "stack": {
                  "name": "stack",
                  "type": "string",
                  "description": "The name of the stack to operate on. Defaults to the current stack"
                },
                "yes": {
                  "name": "yes",
                  "type": "boolean",
                  "description": "Automatically confirm every confirmation prompt"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Delete all the stack's deployment settings"
            },
            "env": {
              "type": "command",
              "flags": {
                "remove": {
                  "name": "remove",
                  "type": "boolean",
                  "description": "whether the key should be removed"
                },
                "secret": {
                  "name": "secret",
                  "type": "boolean",
                  "description": "whether the value should be treated as a secret and be encrypted"
                },
                "stack": {
                  "name": "stack",
                  "type": "string",
                  "description": "The name of the stack to operate on. Defaults to the current stack"
                }
              },
              "arguments": {
                "arguments": [
                  {
                    "name": "key"
                  },
                  {
                    "name": "value"
                  }
                ],
                "requiredArguments": 1
              },
              "description": "Update stack's deployment settings secrets"
            },
            "init": {
              "type": "command",
              "flags": {
                "force": {
                  "name": "force",
                  "type": "boolean",
                  "description": "Forces content to be generated even if it is already configured"
                },
                "git-auth-ssh-private-key": {
                  "name": "git-auth-ssh-private-key",
                  "type": "string",
                  "description": "Git SSH private key"
                },
                "git-auth-ssh-private-key-path": {
                  "name": "git-auth-ssh-private-key-path",
                  "type": "string",
                  "description": "Git SSH private key path"
                },
                "stack": {
                  "name": "stack",
                  "type": "string",
                  "description": "The name of the stack to operate on. Defaults to the current stack"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Initialize the stack's deployment.yaml file"
            },
            "pull": {
              "type": "command",
              "flags": {
                "stack": {
                  "name": "stack",
                  "type": "string",
                  "description": "The name of the stack to operate on. Defaults to the current stack"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Pull the stack's deployment settings from Pulumi Cloud into the deployment.yaml file"
            },
            "push": {
              "type": "command",
              "flags": {
                "stack": {
                  "name": "stack",
                  "type": "string",
                  "description": "The name of the stack to operate on. Defaults to the current stack"
                },
                "yes": {
                  "name": "yes",
                  "type": "boolean",
                  "description": "Automatically confirm every confirmation prompt"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Update stack deployment settings from deployment.yaml"
            }
          }
        }
      }
    },
    "destroy": {
      "type": "command",
      "flags": {
        "client": {
          "name": "client",
          "type": "string",
          "description": "The address of an existing language runtime host to connect to"
        },
        "config": {
          "name": "config",
          "type": "string",
          "description": "Config to use during the destroy and save to the stack config file",
          "repeatable": true
        },
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "config-path": {
          "name": "config-path",
          "type": "boolean",
          "description": "Config keys contain a path to a property in a map or list to set"
        },
        "continue-on-error": {
          "name": "continue-on-error",
          "type": "boolean",
          "description": "Continue to perform the destroy operation despite the occurrence of errors (can also be set with PULUMI_CONTINUE_ON_ERROR env var)"
        },
        "copilot": {
          "name": "copilot",
          "type": "boolean",
          "description": "[DEPRECATED] Use --neo instead. Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_COPILOT environment variable)"
        },
        "debug": {
          "name": "debug",
          "type": "boolean",
          "description": "Print detailed debugging output during resource operations"
        },
        "diff": {
          "name": "diff",
          "type": "boolean",
          "description": "Display operation as a rich diff showing the overall change"
        },
        "exclude": {
          "name": "exclude",
          "type": "string",
          "description": "Specify a resource URN to ignore. These resources will not be updated. Multiple resources can be specified using --exclude urn1 --exclude urn2. Wildcards (*, **) are also supported",
          "repeatable": true
        },
        "exclude-protected": {
          "name": "exclude-protected",
          "type": "boolean",
          "description": "Do not destroy protected resources. Destroy all other resources."
        },
        "exec-agent": {
          "name": "exec-agent",
          "type": "string"
        },
        "exec-kind": {
          "name": "exec-kind",
          "type": "string"
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Serialize the destroy diffs, operations, and overall output as JSON"
        },
        "message": {
          "name": "message",
          "type": "string",
          "description": "Optional message to associate with the destroy operation"
        },
        "neo": {
          "name": "neo",
          "type": "boolean",
          "description": "Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_NEO environment variable)"
        },
        "parallel": {
          "name": "parallel",
          "type": "int",
          "description": "Allow P resource operations to run in parallel at once (1 for no parallelism)."
        },
        "preview-only": {
          "name": "preview-only",
          "type": "boolean",
          "description": "Only show a preview of the destroy, but don't perform the destroy itself"
        },
        "refresh": {
          "name": "refresh",
          "type": "string",
          "description": "Refresh the state of the stack's resources before this update"
        },
        "remove": {
          "name": "remove",
          "type": "boolean",
          "description": "Remove the stack and its config file after all resources in the stack have been deleted"
        },
        "run-program": {
          "name": "run-program",
          "type": "boolean",
          "description": "Run the program to determine up-to-date state for providers to destroy resources"
        },
        "show-config": {
          "name": "show-config",
          "type": "boolean",
          "description": "Show configuration keys and variables"
        },
        "show-full-output": {
          "name": "show-full-output",
          "type": "boolean",
          "description": "Display full length of inputs \u0026 outputs"
        },
        "show-replacement-steps": {
          "name": "show-replacement-steps",
          "type": "boolean",
          "description": "Show detailed resource replacement creates and deletes instead of a single step"
        },
        "show-sames": {
          "name": "show-sames",
          "type": "boolean",
          "description": "Show resources that don't need to be updated because they haven't changed, alongside those that do"
        },
        "skip-preview": {
          "name": "skip-preview",
          "type": "boolean",
          "description": "Do not calculate a preview before performing the destroy"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        },
        "suppress-outputs": {
          "name": "suppress-outputs",
          "type": "boolean",
          "description": "Suppress display of stack outputs (in case they contain sensitive values)"
        },
        "suppress-permalink": {
          "name": "suppress-permalink",
          "type": "string",
          "description": "Suppress display of the state permalink"
        },
        "suppress-progress": {
          "name": "suppress-progress",
          "type": "boolean",
          "description": "Suppress display of periodic progress dots"
        },
        "target": {
          "name": "target",
          "type": "string",
          "description": "Specify a single resource URN to destroy. All resources necessary to destroy this target will also be destroyed. Multiple resources can be specified using: --target urn1 --target urn2. Wildcards (*, **) are also supported",
          "repeatable": true
        },
        "target-dependents": {
          "name": "target-dependents",
          "type": "boolean",
          "description": "Allows destroying of dependent targets discovered but not specified in --target list"
        },
        "yes": {
          "name": "yes",
          "type": "boolean",
          "description": "Automatically approve and perform the destroy after previewing it"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "Destroy all existing resources in the stack, but not the stack itself\n\nDeletes all the resources in the selected stack.  The current state is\nloaded from the associated state file in the workspace.  After running to completion,\nall of this stack's resources and associated state are deleted.\n\nThe stack itself is not deleted. Use `pulumi stack rm` or the \n`--remove` flag to delete the stack and its config file.\n\nWarning: this command is generally irreversible and should be used with great care."
    },
    "env": {
      "type": "menu",
      "executable": false,
      "flags": {
        "env": {
          "name": "env",
          "type": "string",
          "description": "The name of the environment to operate on."
        }
      },
      "commands": {
        "clone": {
          "type": "command",
          "flags": {
            "preserve-access": {
              "name": "preserve-access",
              "type": "boolean",
              "description": "preserve the same team access on the environment being cloned"
            },
            "preserve-env-tags": {
              "name": "preserve-env-tags",
              "type": "boolean",
              "description": "preserve any tags on the environment being cloned"
            },
            "preserve-history": {
              "name": "preserve-history",
              "type": "boolean",
              "description": "preserve history of the environment being cloned"
            },
            "preserve-rev-tags": {
              "name": "preserve-rev-tags",
              "type": "boolean",
              "description": "preserve any tags on the environment revisions being cloned"
            }
          },
          "description": "Clone an existing environment into a new environment.\n\nThis command clones an existing environment with the given identifier into a new environment.\nIf a project is omitted from the new environment identifier the new environment will be created\nwithin the same project as the environment being cloned."
        },
        "diff": {
          "type": "command",
          "flags": {
            "format": {
              "name": "format",
              "type": "string",
              "description": "the output format to use. May be 'dotenv', 'json', 'yaml', 'detailed', or 'shell'"
            },
            "path": {
              "name": "path",
              "type": "string",
              "description": "Show the diff for a specific path"
            },
            "show-secrets": {
              "name": "show-secrets",
              "type": "boolean",
              "description": "Show static secrets in plaintext rather than ciphertext"
            }
          },
          "description": "Show changes between versions\n\nThis command displays the changes between two environments or two versions\nof a single environment.\n\nThe first argument is the base environment for the diff and the second argument\nis the comparison environment. If the environment name portion of the second\nargument is omitted, the name of the base environment is used. If the version portion of\nthe second argument is omitted, the 'latest' tag is used."
        },
        "edit": {
          "type": "command",
          "flags": {
            "draft": {
              "name": "draft",
              "type": "string",
              "description": "set flag without a value (--draft) to create a draft rather than saving changes directly. --draft=\u003cchange-request-id\u003e to update an existing change request."
            },
            "editor": {
              "name": "editor",
              "type": "string",
              "description": "the command to use to edit the environment definition"
            },
            "file": {
              "name": "file",
              "type": "string",
              "description": "the file that contains the updated environment, if any. Pass `-` to read from standard input."
            },
            "show-secrets": {
              "name": "show-secrets",
              "type": "boolean",
              "description": "Show static secrets in plaintext rather than ciphertext"
            }
          },
          "description": "Edit an environment definition\n\nThis command fetches the current definition for the named environment and opens it\nfor editing in an editor. The editor defaults to the value of the VISUAL environment\nvariable. If VISUAL is not set, EDITOR is used. These values are interpreted as\ncommands to which the name of the temporary file used for the environment is appended.\nIf no editor is specified via the --editor flag or environment variables, edit\ndefaults to `vi`."
        },
        "get": {
          "type": "command",
          "flags": {
            "definition": {
              "name": "definition",
              "type": "boolean",
              "description": "Set to print just the definition."
            },
            "show-secrets": {
              "name": "show-secrets",
              "type": "boolean",
              "description": "Show static secrets in plaintext rather than ciphertext"
            },
            "value": {
              "name": "value",
              "type": "string",
              "description": "Set to print just the value in the given format. May be 'dotenv', 'json', 'detailed', 'shell' or 'string'"
            }
          },
          "description": "Get a value within an environment\n\nThis command fetches the current definition for the named environment and gets a\nvalue within it. The path to the value to set is a Pulumi property path. The value\nis printed to stdout as YAML."
        },
        "init": {
          "type": "command",
          "flags": {
            "file": {
              "name": "file",
              "type": "string",
              "description": "the file to use to initialize the environment, if any. Pass `-` to read from standard input."
            }
          },
          "description": "Create an empty environment with the given name, ready for editing\n\nThis command creates an empty environment with the given name. It has no definition,\nbut afterwards it can be edited using the `edit` command.\n\nTo create an environment in an organization when logged in to the Pulumi Cloud,\nprefix the stack name with the organization name and a slash (e.g. 'acmecorp/dev')."
        },
        "ls": {
          "type": "command",
          "flags": {
            "organization": {
              "name": "organization",
              "type": "string",
              "description": "Filter returned environments to those in a specific organization"
            },
            "project": {
              "name": "project",
              "type": "string",
              "description": "Filter returned environments to those in a specific project"
            }
          },
          "description": "List environments\n\nThis command lists environments. All environments you have access to will be listed."
        },
        "open": {
          "type": "command",
          "flags": {
            "draft": {
              "name": "draft",
              "type": "string",
              "description": "open an environment draft with --draft=\u003cchange-request-id\u003e"
            },
            "format": {
              "name": "format",
              "type": "string",
              "description": "the output format to use. May be 'dotenv', 'json', 'yaml', 'detailed', 'shell' or 'string'"
            },
            "lifetime": {
              "name": "lifetime",
              "type": "string",
              "description": "the lifetime of the opened environment in the form HhMm (e.g. 2h, 1h30m, 15m)"
            }
          },
          "description": "Open the environment with the given name and return the result\n\nThis command opens the environment with the given name. The result is written to\nstdout as JSON. If a property path is specified, only retrieves that property."
        },
        "rm": {
          "type": "command",
          "flags": {
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts, and proceed with removal anyway"
            }
          },
          "description": "Remove an environment or a value from an environment\n\nThis command removes an environment or a value from an environment.\nWhen removing an environment, the environment will no longer be available\nonce this command completes."
        },
        "rotate": {
          "type": "command",
          "description": "Rotate secrets in an environment\n\nOptionally accepts any number of Property Paths as additional arguments. If given any paths, will only rotate secrets at those paths."
        },
        "run": {
          "type": "command",
          "flags": {
            "draft": {
              "name": "draft",
              "type": "string",
              "description": "open an environment draft with --draft=\u003cchange-request-id\u003e"
            },
            "interactive": {
              "name": "interactive",
              "type": "boolean",
              "description": "true to treat the command as interactive and disable output filters"
            },
            "lifetime": {
              "name": "lifetime",
              "type": "string",
              "description": "the lifetime of the opened environment"
            }
          },
          "description": "Open the environment with the given name and run a command\n\nThis command opens the environment with the given name and runs the given command.\nIf the opened environment contains a top-level 'environmentVariables' object, each\nkey-value pair in the object is made available to the command as an environment\nvariable. Note that commands are not run in a subshell, so environment variable\nreferences in the command are not expanded by default. You should invoke the command\ninside a shell if you need environment variable expansion:\n\n    run \u003cenvironment-name\u003e -- zsh -c '\"echo $MY_ENV_VAR\"'\n\nThe command to run is assumed to be non-interactive by default and its output\nstreams are filtered to remove any secret values. Use the -i flag to run interactive\ncommands, which will disable filtering.\n\nIt is not strictly required that you pass `--`. The `--` indicates that any\narguments that follow it should be treated as positional arguments instead of flags.\nIt is only required if the arguments to the command you would like to run include\nflags of the form `--flag` or `-f`."
        },
        "set": {
          "type": "command",
          "flags": {
            "draft": {
              "name": "draft",
              "type": "string",
              "description": "set flag without a value (--draft) to create a draft rather than saving changes directly. --draft=\u003cchange-request-id\u003e to update an existing change request."
            },
            "file": {
              "name": "file",
              "type": "string",
              "description": "If set, the value is read from the specified file. Pass `-` to read from standard input."
            },
            "plaintext": {
              "name": "plaintext",
              "type": "boolean",
              "description": "true to leave the value in plaintext"
            },
            "secret": {
              "name": "secret",
              "type": "boolean",
              "description": "true to mark the value as secret"
            },
            "string": {
              "name": "string",
              "type": "boolean",
              "description": "true to treat the value as a string rather than attempting to parse it as YAML"
            }
          },
          "description": "Set a value within an environment\n\nThis command fetches the current definition for the named environment and modifies a\nvalue within it. The path to the value to set is a Pulumi property path. The value\nis interpreted as YAML."
        },
        "tag": {
          "type": "menu",
          "executable": true,
          "flags": {
            "utc": {
              "name": "utc",
              "type": "boolean",
              "description": "display times in UTC"
            }
          },
          "commands": {
            "get": {
              "type": "command",
              "flags": {
                "utc": {
                  "name": "utc",
                  "type": "boolean",
                  "description": "display times in UTC"
                }
              },
              "description": "Get an environment tag\n\nThis command get a tag with the given name on the specified environment."
            },
            "ls": {
              "type": "command",
              "flags": {
                "pager": {
                  "name": "pager",
                  "type": "string",
                  "description": "the command to use to page through the environment's version tags"
                },
                "utc": {
                  "name": "utc",
                  "type": "boolean",
                  "description": "display times in UTC"
                }
              },
              "description": "List environment tags\n\nThis command lists an environment's tags."
            },
            "mv": {
              "type": "command",
              "flags": {
                "utc": {
                  "name": "utc",
                  "type": "boolean",
                  "description": "display times in UTC"
                }
              },
              "description": "Move an environment tag\n\nThis command updates a tag with the given name on the specified environment, changing it's name."
            },
            "rm": {
              "type": "command",
              "description": "Remove an environment tag\n\nThis command removes an environment tag using the tag name."
            }
          }
        },
        "version": {
          "type": "menu",
          "executable": true,
          "flags": {
            "utc": {
              "name": "utc",
              "type": "boolean",
              "description": "display times in UTC"
            }
          },
          "commands": {
            "history": {
              "type": "command",
              "flags": {
                "pager": {
                  "name": "pager",
                  "type": "string",
                  "description": "the command to use to page through the environment's revisions"
                },
                "utc": {
                  "name": "utc",
                  "type": "boolean",
                  "description": "display times in UTC"
                }
              },
              "description": "Show revision history\n\nThis command shows the revision history for an environment. If a version\nis present, the logs will start at the corresponding revision."
            },
            "retract": {
              "type": "command",
              "flags": {
                "reason": {
                  "name": "reason",
                  "type": "string",
                  "description": "the reason for the retraction"
                },
                "replace-with": {
                  "name": "replace-with",
                  "type": "string",
                  "description": "the version to use to replace the retracted revision"
                }
              },
              "description": "Retract a specific revision of an environment\n\nThis command retracts a specific revision of an environment. A retracted\nrevision can no longer be read or opened. Retracting a revision also updates\nany tags that point to the retracted revision to instead point to a\nreplacement revision. If no replacement is specified, the latest non-retracted\nrevision preceding the revision being retracted is used as the replacement.\n\nThe revision pointed to by the `latest` tag may not be retracted. To retract\nthe latest revision of an environment, first update the environment with a new\ndefinition."
            },
            "rollback": {
              "type": "command",
              "flags": {
                "draft": {
                  "name": "draft",
                  "type": "string",
                  "description": "set flag without a value (--draft) to create a draft rather than saving changes directly. --draft=\u003cchange-request-id\u003e to update an existing change request."
                }
              },
              "description": "Roll back to a specific version\n\nThis command rolls an environment's definition back to the specified\nversion. The environment's definition will be replaced with the\ndefinition at that version, creating a new revision."
            },
            "tag": {
              "type": "menu",
              "executable": true,
              "flags": {
                "utc": {
                  "name": "utc",
                  "type": "boolean",
                  "description": "display times in UTC"
                }
              },
              "commands": {
                "ls": {
                  "type": "command",
                  "flags": {
                    "pager": {
                      "name": "pager",
                      "type": "string",
                      "description": "the command to use to page through the environment's version tags"
                    },
                    "utc": {
                      "name": "utc",
                      "type": "boolean",
                      "description": "display times in UTC"
                    }
                  },
                  "description": "List tagged versions\n\nThis command lists an environment's tagged versions."
                },
                "rm": {
                  "type": "command",
                  "description": "Remove a tagged version\n\nThis command removes the tagged version with the given name"
                }
              }
            }
          }
        }
      }
    },
    "gen-completion": {
      "type": "command",
      "arguments": {
        "arguments": [
          {
            "name": "shell",
            "type": "string",
            "usage": "\u003cshell\u003e"
          }
        ],
        "requiredArguments": 1
      },
      "description": "Generate completion scripts for the Pulumi CLI"
    },
    "gen-markdown": {
      "type": "command",
      "arguments": {
        "arguments": [
          {
            "name": "dir"
          }
        ],
        "requiredArguments": 1
      },
      "description": "Generate Pulumi CLI documentation as Markdown (one file per command)"
    },
    "generate-cli-spec": {
      "type": "command",
      "flags": {
        "help": {
          "name": "help",
          "type": "boolean",
          "description": "help for generate-cli-spec"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "Generate a Pulumi CLI specification as JSON"
    },
    "help": {
      "type": "command",
      "description": "Help provides help for any command in the application.\nSimply type pulumi help [path to command] for full details."
    },
    "import": {
      "type": "command",
      "flags": {
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "debug": {
          "name": "debug",
          "type": "boolean",
          "description": "Print detailed debugging output during resource operations"
        },
        "diff": {
          "name": "diff",
          "type": "boolean",
          "description": "Display operation as a rich diff showing the overall change"
        },
        "exec-agent": {
          "name": "exec-agent",
          "type": "string"
        },
        "exec-kind": {
          "name": "exec-kind",
          "type": "string"
        },
        "file": {
          "name": "file",
          "type": "string",
          "description": "The path to a JSON-encoded file containing a list of resources to import"
        },
        "from": {
          "name": "from",
          "type": "string",
          "description": "Invoke a converter to import the resources"
        },
        "generate-code": {
          "name": "generate-code",
          "type": "boolean",
          "description": "Generate resource declaration code for the imported resources"
        },
        "generate-resources": {
          "name": "generate-resources",
          "type": "string",
          "description": "When used with --from, always write a JSON-encoded file containing a list of importable resources discovered by conversion to the specified path"
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Serialize the import diffs, operations, and overall output as JSON"
        },
        "message": {
          "name": "message",
          "type": "string",
          "description": "Optional message to associate with the update operation"
        },
        "out": {
          "name": "out",
          "type": "string",
          "description": "The path to the file that will contain the generated resource declarations"
        },
        "parallel": {
          "name": "parallel",
          "type": "int",
          "description": "Allow P resource operations to run in parallel at once (1 for no parallelism)."
        },
        "parent": {
          "name": "parent",
          "type": "string",
          "description": "The name and URN of the parent resource in the format name=urn, where name is the variable name of the parent resource"
        },
        "preview-only": {
          "name": "preview-only",
          "type": "boolean",
          "description": "Only show a preview of the import, but don't perform the import itself"
        },
        "properties": {
          "name": "properties",
          "type": "string",
          "description": "The property names to use for the import in the format name1,name2",
          "repeatable": true
        },
        "protect": {
          "name": "protect",
          "type": "boolean",
          "description": "Allow resources to be imported with protection from deletion enabled"
        },
        "provider": {
          "name": "provider",
          "type": "string",
          "description": "The name and URN of the provider to use for the import in the format name=urn, where name is the variable name for the provider resource"
        },
        "skip-preview": {
          "name": "skip-preview",
          "type": "boolean",
          "description": "Do not calculate a preview before performing the import"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        },
        "suppress-outputs": {
          "name": "suppress-outputs",
          "type": "boolean",
          "description": "Suppress display of stack outputs (in case they contain sensitive values)"
        },
        "suppress-permalink": {
          "name": "suppress-permalink",
          "type": "string",
          "description": "Suppress display of the state permalink"
        },
        "suppress-progress": {
          "name": "suppress-progress",
          "type": "boolean",
          "description": "Suppress display of periodic progress dots"
        },
        "yes": {
          "name": "yes",
          "type": "boolean",
          "description": "Automatically approve and perform the import after previewing it"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "arg"
          }
        ],
        "variadic": true
      },
      "description": "Import resources into an existing stack.\n\nResources that are not managed by Pulumi can be imported into a Pulumi stack\nusing this command. A definition for each resource will be printed to stdout\nin the language used by the project associated with the stack; these definitions\nshould be added to the Pulumi program. The resources are protected from deletion\nby default.\n\nShould you want to import your resource(s) without protection, you can pass\n`--protect=false` as an argument to the command. This will leave all resources unprotected.\n\nA single resource may be specified in the command line arguments or a set of\nresources may be specified by a JSON file.\n\nIf using the command line args directly, the type token, name, id and optional flags\nmust be provided.  For example:\n\n    pulumi import 'aws:iam/user:User' name id\n\nThe type token and property used for resource lookup are available in the Import section of\nthe resource's API documentation in the Pulumi Registry (https://www.pulumi.com/registry/).\nTo fully specify parent and/or provider, subsitute the \u003curn\u003e for each into the following:\n\n     pulumi import 'aws:iam/user:User' name id --parent 'parent=\u003curn\u003e' --provider 'admin=\u003curn\u003e'\n\nWhen importing multiple resources at once the `--file` option can be used to pass a JSON file\ncontaining multiple resources: \n     pulumi import --file import.json\n\nWhere import.json is a file that matches the following JSON format:\n\n    {\n        \"resources\": [\n            {\n                \"type\": \"aws:ec2/vpc:Vpc\",\n                \"name\": \"application-vpc\",\n                \"id\": \"vpc-0ad77710973388316\"\n            },\n            ...\n            {\n                ...\n            }\n        ],\n    }\n\nThe full import file schema references can be found in the [import documentation](https://www.pulumi.com/docs/iac/adopting-pulumi/import/#bulk-import-operations).\n\nThe import JSON file can be generated from a Pulumi program by running\n\n    pulumi preview --import-file import.json\n\nThis will create entries for all resources that need creating from the preview, filling\nin the name, type, parent and provider information and just requiring you to fill in the\nresource IDs."
    },
    "install": {
      "type": "command",
      "flags": {
        "no-dependencies": {
          "name": "no-dependencies",
          "type": "boolean",
          "description": "Skip installing dependencies"
        },
        "no-plugins": {
          "name": "no-plugins",
          "type": "boolean",
          "description": "Skip installing plugins"
        },
        "parallel": {
          "name": "parallel",
          "type": "int",
          "description": "The max number of concurrent installs to perform. Parallelism of less then 1 implies unbounded parallelism"
        },
        "reinstall": {
          "name": "reinstall",
          "type": "boolean",
          "description": "Reinstall a plugin even if it already exists"
        },
        "use-language-version-tools": {
          "name": "use-language-version-tools",
          "type": "boolean",
          "description": "Use language version tools to setup and install the language runtime"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "Install packages and plugins for the current program or policy pack.\n\nThis command is used to manually install packages and plugins required by your program or policy pack.\nIf your Pulumi.yaml file contains a 'packages' section, this command will automatically install\nSDKs for all packages declared in that section, similar to the 'pulumi package add' command."
    },
    "login": {
      "type": "command",
      "flags": {
        "cloud-url": {
          "name": "cloud-url",
          "type": "string",
          "description": "A cloud URL to log in to"
        },
        "default-org": {
          "name": "default-org",
          "type": "string",
          "description": "A default org to associate with the login. Please note, currently, only the managed and self-hosted backends support organizations"
        },
        "insecure": {
          "name": "insecure",
          "type": "boolean",
          "description": "Allow insecure server connections when using SSL"
        },
        "interactive": {
          "name": "interactive",
          "type": "boolean",
          "description": "Show interactive login options based on known accounts"
        },
        "local": {
          "name": "local",
          "type": "boolean",
          "description": "Use Pulumi in local-only mode"
        },
        "oidc-expiration": {
          "name": "oidc-expiration",
          "type": "string",
          "description": "The expiration for the cloud backend access token in duration format (e.g. '15m', '24h')"
        },
        "oidc-org": {
          "name": "oidc-org",
          "type": "string",
          "description": "The organization to use for OIDC token exchange audience"
        },
        "oidc-team": {
          "name": "oidc-team",
          "type": "string",
          "description": "The team when exchanging for a team token"
        },
        "oidc-token": {
          "name": "oidc-token",
          "type": "string",
          "description": "An OIDC token to exchange for a cloud backend access token. Can be either a raw token or a file path prefixed with 'file://'."
        },
        "oidc-user": {
          "name": "oidc-user",
          "type": "string",
          "description": "The user when exchanging for a personal token"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "url"
          }
        ]
      },
      "description": "Log in to the Pulumi Cloud.\n\nThe Pulumi Cloud manages your stack's state reliably. Simply run\n\n    $ pulumi login\n\nand this command will prompt you for an access token, including a way to launch your web browser to\neasily obtain one. You can script by using `PULUMI_ACCESS_TOKEN` environment variable.\n\nBy default, this will log in to the managed Pulumi Cloud backend.\nIf you prefer to log in to a self-hosted Pulumi Cloud backend, specify a URL. For example, run\n\n    $ pulumi login https://api.pulumi.acmecorp.com\n\nto log in to a self-hosted Pulumi Cloud running at the api.pulumi.acmecorp.com domain.\n\nFor `https://` URLs, the CLI will speak REST to a Pulumi Cloud that manages state and concurrency control.\nYou can specify a default org to use when logging into the Pulumi Cloud backend or a self-hosted Pulumi Cloud.\n\nIf you prefer to operate Pulumi independently of a Pulumi Cloud, and entirely local to your computer,\npass `file://\u003cpath\u003e`, where `\u003cpath\u003e` will be where state checkpoints will be stored. For instance,\n\n    $ pulumi login file://~\n\nwill store your state information on your computer underneath `~/.pulumi`. It is then up to you to\nmanage this state, including backing it up, using it in a team environment, and so on.\n\nAs a shortcut, you may pass --local to use your home directory (this is an alias for `file://~`):\n\n    $ pulumi login --local\n\nAdditionally, you may leverage supported object storage backends from one of the cloud providers to manage the state independent of the Pulumi Cloud. For instance,\n\nAWS S3:\n\n    $ pulumi login s3://my-pulumi-state-bucket\n\nGCP GCS:\n\n    $ pulumi login gs://my-pulumi-state-bucket\n\nAzure Blob:\n\n    $ pulumi login azblob://my-pulumi-state-bucket\n\nPostgreSQL:\n\n    $ pulumi login postgres://username:password@hostname:5432/database"
    },
    "logout": {
      "type": "command",
      "flags": {
        "all": {
          "name": "all",
          "type": "boolean",
          "description": "Logout of all backends"
        },
        "cloud-url": {
          "name": "cloud-url",
          "type": "string",
          "description": "A cloud URL to log out of (defaults to current cloud)"
        },
        "local": {
          "name": "local",
          "type": "boolean",
          "description": "Log out of using local mode"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "url"
          }
        ]
      },
      "description": "Log out of the Pulumi Cloud.\n\nThis command deletes stored credentials on the local machine for a single login.\n\nBecause you may be logged into multiple backends simultaneously, you can optionally pass\na specific URL argument, formatted just as you logged in, to log out of a specific one.\nIf no URL is provided, you will be logged out of the current backend.\n\nIf you would like to log out of all backends simultaneously, you can pass `--all`,\n\n    $ pulumi logout --all"
    },
    "logs": {
      "type": "command",
      "flags": {
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "follow": {
          "name": "follow",
          "type": "boolean",
          "description": "Follow the log stream in real time (like tail -f)"
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Emit output as JSON"
        },
        "resource": {
          "name": "resource",
          "type": "string",
          "description": "Only return logs for the requested resource ('name', 'type::name' or full URN).  Defaults to returning all logs."
        },
        "since": {
          "name": "since",
          "type": "string",
          "description": "Only return logs newer than a relative duration ('5s', '2m', '3h') or absolute timestamp.  Defaults to returning the last 1 hour of logs."
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "[EXPERIMENTAL] Show aggregated resource logs for a stack\n\nThis command aggregates log entries associated with the resources in a stack from the corresponding\nprovider. For example, for AWS resources, the `pulumi logs` command will query\nCloudWatch Logs for log data relevant to resources in a stack."
    },
    "new": {
      "type": "command",
      "flags": {
        "ai": {
          "name": "ai",
          "type": "string",
          "description": "Prompt to use for Pulumi AI"
        },
        "config": {
          "name": "config",
          "type": "string",
          "description": "Config to save",
          "repeatable": true
        },
        "config-path": {
          "name": "config-path",
          "type": "boolean",
          "description": "Config keys contain a path to a property in a map or list to set"
        },
        "description": {
          "name": "description",
          "type": "string",
          "description": "The project description; if not specified, a prompt will request it"
        },
        "dir": {
          "name": "dir",
          "type": "string",
          "description": "The location to place the generated project; if not specified, the current directory is used"
        },
        "force": {
          "name": "force",
          "type": "boolean",
          "description": "Forces content to be generated even if it would change existing files"
        },
        "generate-only": {
          "name": "generate-only",
          "type": "boolean",
          "description": "Generate the project only; do not create a stack, save config, or install dependencies"
        },
        "language": {
          "name": "language",
          "type": "string",
          "description": "Language to use for Pulumi AI (must be one of TypeScript, JavaScript, Python, Go, C#, Java, or YAML)"
        },
        "list-templates": {
          "name": "list-templates",
          "type": "boolean",
          "description": "List locally installed templates and exit"
        },
        "name": {
          "name": "name",
          "type": "string",
          "description": "The project name; if not specified, a prompt will request it"
        },
        "offline": {
          "name": "offline",
          "type": "boolean",
          "description": "Use locally cached templates without making any network requests"
        },
        "remote-stack-config": {
          "name": "remote-stack-config",
          "type": "boolean",
          "description": "Store stack configuration remotely"
        },
        "runtime-options": {
          "name": "runtime-options",
          "type": "string",
          "description": "Additional options for the language runtime (format: key1=value1,key2=value2)",
          "repeatable": true
        },
        "secrets-provider": {
          "name": "secrets-provider",
          "type": "string",
          "description": "The type of the provider that should be used to encrypt and decrypt secrets (possible choices: default, passphrase, awskms, azurekeyvault, gcpkms, hashivault)"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The stack name; either an existing stack or stack to create; if not specified, a prompt will request it"
        },
        "template-mode": {
          "name": "template-mode",
          "type": "boolean",
          "description": "Run in template mode, which will skip prompting for AI or Template functionality"
        },
        "yes": {
          "name": "yes",
          "type": "boolean",
          "description": "Skip prompts and proceed with default values"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "template-or-url",
            "usage": "[template|url]"
          }
        ]
      },
      "description": "Create a new Pulumi project and stack from a template.\n\nTo create a project from a specific template, pass the template name (such as `aws-typescript`\nor `azure-python`). If no template name is provided, a list of suggested templates will be presented\nwhich can be selected interactively.\nFor testing, a path to a local template may be passed instead (such as `~/templates/aws-typescript`)\n\nBy default, a stack created using the pulumi.com backend will use the pulumi.com secrets\nprovider and a stack created using the local or cloud object storage backend will use the\n`passphrase` secrets provider.  A different secrets provider can be selected by passing the\n`--secrets-provider` flag.\n\nTo use the `passphrase` secrets provider with the pulumi.com backend, use:\n* `pulumi new --secrets-provider=passphrase`\n\nTo use a cloud secrets provider with any backend, use one of the following:\n* `pulumi new --secrets-provider=\"awskms://alias/ExampleAlias?region=us-east-1\"`\n* `pulumi new --secrets-provider=\"awskms://1234abcd-12ab-34cd-56ef-1234567890ab?region=us-east-1\"`\n* `pulumi new --secrets-provider=\"azurekeyvault://mykeyvaultname.vault.azure.net/keys/mykeyname\"`\n* `pulumi new --secrets-provider=\"gcpkms://projects/p/locations/l/keyRings/r/cryptoKeys/k\"`\n* `pulumi new --secrets-provider=\"hashivault://mykey\"`\n\nTo create a project from a specific source control location, pass the url as follows e.g.\n* `pulumi new https://gitlab.com/\u003cuser\u003e/\u003crepo\u003e`\n* `pulumi new https://bitbucket.org/\u003cuser\u003e/\u003crepo\u003e`\n* `pulumi new https://github.com/\u003cuser\u003e/\u003crepo\u003e`\n\n  Note: If the URL doesn't follow the usual scheme of the given host (e.g. for GitLab subprojects)\n        you can append `.git` to the repository to disambiguate and point to the correct repository.\n        For example `https://gitlab.com/\u003cproject\u003e/\u003csubproject\u003e/\u003crepository\u003e.git`.\n\nTo create the project from a branch of a specific source control location, pass the url to the branch, e.g.\n* `pulumi new https://gitlab.com/\u003cuser\u003e/\u003crepo\u003e/tree/\u003cbranch\u003e`\n* `pulumi new https://bitbucket.org/\u003cuser\u003e/\u003crepo\u003e/tree/\u003cbranch\u003e`\n* `pulumi new https://github.com/\u003cuser\u003e/\u003crepo\u003e/tree/\u003cbranch\u003e`\n\nTo use a private repository as a template source, provide an HTTPS or SSH URL with relevant credentials.\nEnsure your SSH agent has the correct identity (ssh-add) or you may be prompted for your key's passphrase.\n* `pulumi new git@github.com:\u003cuser\u003e/\u003cprivate-repo\u003e`\n* `pulumi new https://\u003cuser\u003e:\u003cpassword\u003e@\u003chostname\u003e/\u003cproject\u003e/\u003crepo\u003e`\n* `pulumi new \u003cuser\u003e@\u003chostname\u003e:\u003cproject\u003e/\u003crepo\u003e`\n* `PULUMI_GITSSH_PASSPHRASE=\u003cpassphrase\u003e pulumi new ssh://\u003cuser\u003e@\u003chostname\u003e/\u003cproject\u003e/\u003crepo\u003e`\nTo create a project using Pulumi AI, either select `ai` from the first selection, or provide any of the following:\n* `pulumi new --ai \"\u003cprompt\u003e\"`\n* `pulumi new --language \u003clanguage\u003e`\n* `pulumi new --ai \"\u003cprompt\u003e\" --language \u003clanguage\u003e`\nAny missing but required information will be prompted for."
    },
    "org": {
      "type": "menu",
      "executable": true,
      "commands": {
        "get-default": {
          "type": "command",
          "arguments": {
            "arguments": []
          },
          "description": "Get the default org for the current backend.\n\nThis command is used to get the default organization for which and stacks are created in the current backend.\n\nCurrently, only the managed and self-hosted backends support organizations."
        },
        "search": {
          "type": "menu",
          "executable": true,
          "flags": {
            "delimiter": {
              "name": "delimiter",
              "type": "string",
              "description": "Delimiter to use when rendering CSV output."
            },
            "org": {
              "name": "org",
              "type": "string",
              "description": "Name of the organization to search. Defaults to the current user's default organization."
            },
            "output": {
              "name": "output",
              "type": "string",
              "description": "Output format. Supported formats are 'table', 'json', 'csv', and 'yaml'."
            },
            "query": {
              "name": "query",
              "type": "string",
              "description": "A Pulumi Query to send to Pulumi Cloud for resource search.May be formatted as a single query, or multiple:\n\t-q \"type:aws:s3/bucketv2:BucketV2 modified:\u003e=2023-09-01\"\n\t-q \"type:aws:s3/bucketv2:BucketV2\" -q \"modified:\u003e=2023-09-01\"\nSee https://www.pulumi.com/docs/pulumi-cloud/insights/search/#query-syntax for syntax reference.",
              "repeatable": true
            },
            "web": {
              "name": "web",
              "type": "boolean",
              "description": "Open the search results in a web browser."
            }
          },
          "commands": {
            "ai": {
              "type": "command",
              "flags": {
                "delimiter": {
                  "name": "delimiter",
                  "type": "string",
                  "description": "Delimiter to use when rendering CSV output."
                },
                "org": {
                  "name": "org",
                  "type": "string",
                  "description": "Organization name to search within"
                },
                "output": {
                  "name": "output",
                  "type": "string",
                  "description": "Output format. Supported formats are 'table', 'json', 'csv' and 'yaml'."
                },
                "query": {
                  "name": "query",
                  "type": "string",
                  "description": "Plaintext natural language query"
                },
                "web": {
                  "name": "web",
                  "type": "boolean",
                  "description": "Open the search results in a web browser."
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "Search for resources in Pulumi Cloud using Pulumi AI"
            }
          }
        },
        "set-default": {
          "type": "command",
          "arguments": {
            "arguments": [
              {
                "name": "name"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Set the local default organization for the current backend.\n\nThis command is used to set your local default organization in which to create \nprojects and stacks for the current backend.\n\nCurrently, only the managed and self-hosted backends support organizations. If you try and set a default organization for a backend that does not \nsupport create organizations, then an error will be returned by the CLI"
        }
      }
    },
    "package": {
      "type": "menu",
      "executable": false,
      "commands": {
        "add": {
          "type": "command",
          "arguments": {
            "arguments": [
              {
                "name": "provider",
                "usage": "\u003cprovider|schema|path\u003e"
              },
              {
                "name": "provider-parameter"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "Add a package to your Pulumi project or plugin.\n\nThis command locally generates an SDK in the currently selected Pulumi language,\nadds the package to your project configuration file (Pulumi.yaml or\nPulumiPlugin.yaml), and prints instructions on how to use it in your project.\nThe SDK is based on a Pulumi package schema extracted from a given resource\nplugin or provided directly.\n\nThe \u003cprovider\u003e argument can be specified in one of the following ways:\n\n- When \u003cprovider\u003e is specified as a PLUGIN[@VERSION] reference, Pulumi attempts to\n  resolve a resource plugin first, installing it on-demand, similarly to:\n\n    pulumi plugin install resource PLUGIN [VERSION]\n\n- When \u003cprovider\u003e is specified as a local path, Pulumi executes the provider\n  binary to extract its package schema:\n\n    pulumi package add ./my-provider\n\n- When \u003cprovider\u003e is a path to a local file with a '.json', '.yml' or '.yaml'\n  extension, Pulumi package schema is read from it directly:\n\n    pulumi package add ./my/schema.json\n\n- When \u003cprovider\u003e is a reference to a Git repo, Pulumi clones the repo and\n  executes the source. Optionally a version can be specified.  It can either\n  be a tag (in semver format), or a Git commit hash.  By default the latest\n  tag (by semver version), or if not available the latest commit on the\n  default branch is used. Paths can be disambiguated from the repo name by\n  appending '.git' to the repo URL, followed by the path to the package:\n\n    pulumi package add example.org/org/repo.git/path[@\u003cversion\u003e]\n\nFor parameterized providers, parameters may be specified as additional\narguments. The exact format of parameters is provider-specific; consult the\nprovider's documentation for more information. If the parameters include flags\nthat begin with dashes, you may need to use '--' to separate the provider name\nfrom the parameters, as in:\n\n  pulumi package add \u003cprovider\u003e -- --provider-parameter-flag value"
        },
        "delete": {
          "type": "command",
          "flags": {
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts, and proceed with deletion anyway"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "package",
                "usage": "\u003csource\u003e/\u003cpublisher\u003e/\u003cname\u003e@\u003cversion\u003e"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Delete a package version from the Pulumi Registry.\n\nThe package version must be specified in the format:\n  [[\u003csource\u003e/]\u003cpublisher\u003e/]\u003cname\u003e[@\u003cversion\u003e]\n\nExample:\n  pulumi package delete private/myorg/my-package@1.0.0\n\nWarning: If this is the only version of the package, the entire package\nwill be removed. This action cannot be undone.\n\nYou must have publish permissions for the package to delete it."
        },
        "gen-sdk": {
          "type": "command",
          "flags": {
            "language": {
              "name": "language",
              "type": "string",
              "description": "The SDK language to generate: [nodejs|python|go|dotnet|java|all]"
            },
            "local": {
              "name": "local",
              "type": "boolean",
              "description": "Generate an SDK appropriate for local usage"
            },
            "out": {
              "name": "out",
              "type": "string",
              "description": "The directory to write the SDK to"
            },
            "overlays": {
              "name": "overlays",
              "type": "string",
              "description": "A folder of extra overlay files to copy to the generated SDK"
            },
            "version": {
              "name": "version",
              "type": "string",
              "description": "The provider plugin version to generate the SDK for"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "schema-source"
              },
              {
                "name": "provider-parameter"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "Generate SDK(s) from a package or schema.\n\n\u003cschema_source\u003e can be a package name or the path to a plugin binary or folder.\nIf a folder either the plugin binary must match the folder name (e.g. 'aws' and 'pulumi-resource-aws') or it must have a PulumiPlugin.yaml file specifying the runtime to use."
        },
        "get-mapping": {
          "type": "command",
          "flags": {
            "out": {
              "name": "out",
              "type": "string",
              "description": "The file to write the mapping data to"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "key"
              },
              {
                "name": "schema-source"
              },
              {
                "name": "provider-key"
              },
              {
                "name": "provider-parameter"
              }
            ],
            "requiredArguments": 2,
            "variadic": true
          },
          "description": "Get the mapping information for a given key from a package.\n\n\u003cschema_source\u003e can be a package name or the path to a plugin binary. [provider key]\nis the name of the source provider (e.g. \"terraform\", if a mapping was being requested\nfrom Terraform to Pulumi). If you need to pass parameters, you must provide a provider\nkey. In the event that you wish to pass none, you must therefore explicitly pass an\nempty string."
        },
        "get-schema": {
          "type": "command",
          "arguments": {
            "arguments": [
              {
                "name": "schema-source"
              },
              {
                "name": "provider-parameter"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "Get the schema.json from a package.\n\n\u003cschema_source\u003e can be a package name or the path to a plugin binary or folder.\nIf a folder either the plugin binary must match the folder name (e.g. 'aws' and 'pulumi-resource-aws') or it must have a PulumiPlugin.yaml file specifying the runtime to use."
        },
        "info": {
          "type": "command",
          "flags": {
            "function": {
              "name": "function",
              "type": "string",
              "description": "Function name"
            },
            "module": {
              "name": "module",
              "type": "string",
              "description": "Module name"
            },
            "resource": {
              "name": "resource",
              "type": "string",
              "description": "Resource name"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "provider",
                "usage": "\u003cprovider|schema|path\u003e"
              },
              {
                "name": "provider-parameter"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "Show information about a package\n\nThis command shows information about a package, its modules and detailed resource info.\n\nThe \u003cprovider\u003e argument can be specified in the same way as in 'pulumi package add'."
        },
        "pack-sdk": {
          "type": "command",
          "arguments": {
            "arguments": [
              {
                "name": "language"
              },
              {
                "name": "path"
              }
            ],
            "requiredArguments": 2
          },
          "description": "Pack a package SDK to a language specific artifact."
        },
        "publish": {
          "type": "command",
          "flags": {
            "installation-configuration": {
              "name": "installation-configuration",
              "type": "string",
              "description": "Path to the installation configuration markdown file"
            },
            "publisher": {
              "name": "publisher",
              "type": "string",
              "description": "The publisher of the package (e.g., 'pulumi'). Defaults to the publisher set in the package schema or the default organization in your pulumi config."
            },
            "readme": {
              "name": "readme",
              "type": "string",
              "description": "Path to the package readme/index markdown file"
            },
            "source": {
              "name": "source",
              "type": "string",
              "description": "The origin of the package (e.g., 'pulumi', 'private', 'opentofu'). Defaults to 'private'."
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "provider",
                "usage": "\u003cprovider|schema\u003e"
              },
              {
                "name": "provider-parameter"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "Publish a package to the Private Registry.\n\nThis command publishes a package to the Private Registry. The package can be a provider or a schema.\n\nWhen \u003cprovider\u003e is specified as a PLUGIN[@VERSION] reference, Pulumi attempts to resolve a resource plugin first, installing it on-demand, similarly to:\n\n  pulumi plugin install resource PLUGIN [VERSION]\n\nWhen \u003cprovider\u003e is specified as a local path, Pulumi executes the provider binary to extract its package schema.\n\nFor parameterized providers, parameters may be specified as additional arguments. The exact format of parameters is provider-specific; consult the provider's documentation for more information. If the parameters include flags that begin with dashes, you may need to use '--' to separate the provider name from the parameters, as in:\n\n  pulumi package publish \u003cprovider\u003e --readme ./README.md -- --provider-parameter-flag value\n\nWhen \u003cschema\u003e is a path to a local file with a '.json', '.yml' or '.yaml' extension, Pulumi package schema is read from it directly:\n\n  pulumi package publish ./my/schema.json --readme ./README.md"
        },
        "publish-sdk": {
          "type": "command",
          "flags": {
            "path": {
              "name": "path",
              "type": "string",
              "description": "The path to the root of your package.\n\tExample: ./sdk/nodejs\n\t"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "language"
              }
            ]
          },
          "description": "Publish a package SDK to supported package registries."
        }
      }
    },
    "plugin": {
      "type": "menu",
      "executable": false,
      "commands": {
        "install": {
          "type": "command",
          "flags": {
            "checksum": {
              "name": "checksum",
              "type": "string",
              "description": "The expected SHA256 checksum for the plugin archive"
            },
            "exact": {
              "name": "exact",
              "type": "boolean",
              "description": "Force installation of an exact version match (usually \u003e= is accepted)"
            },
            "file": {
              "name": "file",
              "type": "string",
              "description": "Install a plugin from a binary, folder or tarball, instead of downloading it"
            },
            "reinstall": {
              "name": "reinstall",
              "type": "boolean",
              "description": "Reinstall a plugin even if it already exists"
            },
            "server": {
              "name": "server",
              "type": "string",
              "description": "A URL to download plugins from"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "kind"
              },
              {
                "name": "name"
              },
              {
                "name": "version"
              }
            ]
          },
          "description": "Install one or more plugins.\n\nThis command is used to manually install plugins required by your program. It\nmay be run with a specific KIND, NAME, and optionally, VERSION, or by omitting\nthese arguments and letting Pulumi compute the set of plugins required by the\ncurrent project. When Pulumi computes the download set automatically, it may\ndownload more plugins than are strictly necessary.\n\nIf VERSION is specified, it cannot be a range; it must be a specific number.\nIf VERSION is unspecified, Pulumi will attempt to look up the latest version of\nthe plugin, though the result is not guaranteed."
        },
        "ls": {
          "type": "command",
          "flags": {
            "json": {
              "name": "json",
              "type": "boolean",
              "description": "Emit output as JSON"
            },
            "project": {
              "name": "project",
              "type": "boolean",
              "description": "List only the plugins used by the current project"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "List plugins"
        },
        "rm": {
          "type": "command",
          "flags": {
            "all": {
              "name": "all",
              "type": "boolean",
              "description": "Remove all plugins"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts, and proceed with removal anyway"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "kind"
              },
              {
                "name": "name"
              },
              {
                "name": "version"
              }
            ]
          },
          "description": "Remove one or more plugins from the download cache.\n\nSpecify KIND, NAME, and/or VERSION to narrow down what will be removed.\nIf none are specified, the entire cache will be cleared.  If only KIND and\nNAME are specified, but not VERSION, all versions of the plugin with the\ngiven KIND and NAME will be removed.  VERSION may be a range.\n\nThis removal cannot be undone.  If a deleted plugin is subsequently required\nin order to execute a Pulumi program, it must be re-downloaded and installed\nusing the plugin install command."
        },
        "run": {
          "type": "command",
          "flags": {
            "kind": {
              "name": "kind",
              "type": "string",
              "description": "The plugin kind"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "name-or-path",
                "usage": "path|name[@version]"
              },
              {
                "name": "args"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "[EXPERIMENTAL] Run a command on a plugin binary.\n\nDirectly executes a plugin binary, if VERSION is not specified the latest installed plugin will be used."
        }
      }
    },
    "policy": {
      "type": "menu",
      "executable": false,
      "commands": {
        "disable": {
          "type": "command",
          "flags": {
            "policy-group": {
              "name": "policy-group",
              "type": "string",
              "description": "The Policy Group for which the Policy Pack will be disabled; if not specified, the default Policy Group is used"
            },
            "version": {
              "name": "version",
              "type": "string",
              "description": "The version of the Policy Pack that will be disabled; if not specified, any enabled version of the Policy Pack will be disabled"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "policy-pack",
                "usage": "\u003corg-name\u003e/\u003cpolicy-pack-name\u003e"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Disable a Policy Pack for a Pulumi organization"
        },
        "enable": {
          "type": "command",
          "flags": {
            "config": {
              "name": "config",
              "type": "string",
              "description": "The file path for the Policy Pack configuration file"
            },
            "policy-group": {
              "name": "policy-group",
              "type": "string",
              "description": "The Policy Group for which the Policy Pack will be enabled; if not specified, the default Policy Group is used"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "policy-pack",
                "usage": "\u003corg-name\u003e/\u003cpolicy-pack-name\u003e"
              },
              {
                "name": "version",
                "usage": "\u003clatest|version\u003e"
              }
            ],
            "requiredArguments": 2
          },
          "description": "Enable a Policy Pack for a Pulumi organization. Can specify latest to enable the latest version of the Policy Pack or a specific version number."
        },
        "group": {
          "type": "menu",
          "executable": false,
          "commands": {
            "ls": {
              "type": "command",
              "flags": {
                "json": {
                  "name": "json",
                  "type": "boolean",
                  "description": "Emit output as JSON"
                }
              },
              "arguments": {
                "arguments": [
                  {
                    "name": "org-name"
                  }
                ]
              },
              "description": "List all Policy Groups for a Pulumi organization"
            }
          }
        },
        "install": {
          "type": "command",
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "Install required policy packs for a stack.\n\nThis command installs the policy packs required by the stack's organization."
        },
        "ls": {
          "type": "command",
          "flags": {
            "json": {
              "name": "json",
              "type": "boolean",
              "description": "Emit output as JSON"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "org-name"
              }
            ]
          },
          "description": "List all Policy Packs for a Pulumi organization"
        },
        "new": {
          "type": "command",
          "flags": {
            "dir": {
              "name": "dir",
              "type": "string",
              "description": "The location to place the generated Policy Pack; if not specified, the current directory is used"
            },
            "force": {
              "name": "force",
              "type": "boolean",
              "description": "Forces content to be generated even if it would change existing files"
            },
            "generate-only": {
              "name": "generate-only",
              "type": "boolean",
              "description": "Generate the Policy Pack only; do not install dependencies"
            },
            "offline": {
              "name": "offline",
              "type": "boolean",
              "description": "Use locally cached templates without making any network requests"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "template",
                "usage": "[template|url]"
              }
            ]
          },
          "description": "Create a new Pulumi Policy Pack from a template.\n\nTo create a Policy Pack from a specific template, pass the template name (such as `aws-typescript`\nor `azure-python`).  If no template name is provided, a list of suggested templates will be presented\nwhich can be selected interactively.\n\nOnce you're done authoring the Policy Pack, you will need to publish the pack to your organization.\nOnly organization administrators can publish a Policy Pack."
        },
        "publish": {
          "type": "command",
          "arguments": {
            "arguments": [
              {
                "name": "org-name"
              }
            ]
          },
          "description": "Publish a Policy Pack to the Pulumi Cloud\n\nIf an organization name is not specified, the default org (if set) or the current user account is used."
        },
        "rm": {
          "type": "command",
          "flags": {
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts, and proceed with removal anyway"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "policy-pack",
                "usage": "\u003corg-name\u003e/\u003cpolicy-pack-name\u003e"
              },
              {
                "name": "version",
                "usage": "\u003call|version\u003e"
              }
            ],
            "requiredArguments": 2
          },
          "description": "Removes a Policy Pack from a Pulumi organization. The Policy Pack must be disabled from all Policy Groups before it can be removed."
        },
        "validate-config": {
          "type": "command",
          "flags": {
            "config": {
              "name": "config",
              "type": "string",
              "description": "The file path for the Policy Pack configuration file"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "policy-pack",
                "usage": "\u003corg-name\u003e/\u003cpolicy-pack-name\u003e"
              },
              {
                "name": "version"
              }
            ],
            "requiredArguments": 2
          },
          "description": "Validate a Policy Pack configuration against the configuration schema of the specified version."
        }
      }
    },
    "preview": {
      "type": "command",
      "flags": {
        "attach-debugger": {
          "name": "attach-debugger",
          "type": "string",
          "description": "Enable the ability to attach a debugger to the program and source based plugins being executed. Can limit debug type to 'program', 'plugins', 'plugin:\u003cname\u003e' or 'all'.",
          "repeatable": true
        },
        "client": {
          "name": "client",
          "type": "string",
          "description": "The address of an existing language runtime host to connect to"
        },
        "config": {
          "name": "config",
          "type": "string",
          "description": "Config to use during the preview and save to the stack config file",
          "repeatable": true
        },
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "config-path": {
          "name": "config-path",
          "type": "boolean",
          "description": "Config keys contain a path to a property in a map or list to set"
        },
        "copilot": {
          "name": "copilot",
          "type": "boolean",
          "description": "[DEPRECATED] Use --neo instead. Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_COPILOT environment variable)"
        },
        "debug": {
          "name": "debug",
          "type": "boolean",
          "description": "Print detailed debugging output during resource operations"
        },
        "diff": {
          "name": "diff",
          "type": "boolean",
          "description": "Display operation as a rich diff showing the overall change"
        },
        "exclude": {
          "name": "exclude",
          "type": "string",
          "description": "Specify a resource URN to ignore. These resources will not be updated. Multiple resources can be specified using --exclude urn1 --exclude urn2. Wildcards (*, **) are also supported",
          "repeatable": true
        },
        "exclude-dependents": {
          "name": "exclude-dependents",
          "type": "boolean",
          "description": "Allow ignoring of dependent targets discovered but not specified in --exclude list"
        },
        "exec-agent": {
          "name": "exec-agent",
          "type": "string"
        },
        "exec-kind": {
          "name": "exec-kind",
          "type": "string"
        },
        "expect-no-changes": {
          "name": "expect-no-changes",
          "type": "boolean",
          "description": "Return an error if any changes are proposed by this preview"
        },
        "import-file": {
          "name": "import-file",
          "type": "string",
          "description": "Save any creates seen during the preview into an import file to use with 'pulumi import'"
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Serialize the preview diffs, operations, and overall output as JSON. Set PULUMI_ENABLE_STREAMING_JSON_PREVIEW to stream JSON events instead."
        },
        "message": {
          "name": "message",
          "type": "string",
          "description": "Optional message to associate with the preview operation"
        },
        "neo": {
          "name": "neo",
          "type": "boolean",
          "description": "Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_NEO environment variable)"
        },
        "parallel": {
          "name": "parallel",
          "type": "int",
          "description": "Allow P resource operations to run in parallel at once (1 for no parallelism)."
        },
        "policy-pack": {
          "name": "policy-pack",
          "type": "string",
          "description": "Run one or more policy packs as part of this update",
          "repeatable": true
        },
        "policy-pack-config": {
          "name": "policy-pack-config",
          "type": "string",
          "description": "Path to JSON file containing the config for the policy pack of the corresponding \"--policy-pack\" flag",
          "repeatable": true
        },
        "refresh": {
          "name": "refresh",
          "type": "string",
          "description": "Refresh the state of the stack's resources before this update"
        },
        "replace": {
          "name": "replace",
          "type": "string",
          "description": "Specify resources to replace. Multiple resources can be specified using --replace urn1 --replace urn2",
          "repeatable": true
        },
        "run-program": {
          "name": "run-program",
          "type": "boolean",
          "description": "Run the program to determine up-to-date state for providers to refresh resources, this only applies if --refresh is set"
        },
        "save-plan": {
          "name": "save-plan",
          "type": "string",
          "description": "[PREVIEW] Save the operations proposed by the preview to a plan file at the given path"
        },
        "show-config": {
          "name": "show-config",
          "type": "boolean",
          "description": "Show configuration keys and variables"
        },
        "show-full-output": {
          "name": "show-full-output",
          "type": "boolean",
          "description": "Display full length of inputs \u0026 outputs"
        },
        "show-policy-remediations": {
          "name": "show-policy-remediations",
          "type": "boolean",
          "description": "Show per-resource policy remediation details instead of a summary"
        },
        "show-reads": {
          "name": "show-reads",
          "type": "boolean",
          "description": "Show resources that are being read in, alongside those being managed directly in the stack"
        },
        "show-replacement-steps": {
          "name": "show-replacement-steps",
          "type": "boolean",
          "description": "Show detailed resource replacement creates and deletes instead of a single step"
        },
        "show-sames": {
          "name": "show-sames",
          "type": "boolean",
          "description": "Show resources that needn't be updated because they haven't changed, alongside those that do"
        },
        "show-secrets": {
          "name": "show-secrets",
          "type": "boolean",
          "description": "Show secrets in plaintext in the CLI output, if used with --save-plan the secrets will also be shown in the plan file. Defaults to `false`"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        },
        "suppress-outputs": {
          "name": "suppress-outputs",
          "type": "boolean",
          "description": "Suppress display of stack outputs (in case they contain sensitive values)"
        },
        "suppress-permalink": {
          "name": "suppress-permalink",
          "type": "string",
          "description": "Suppress display of the state permalink"
        },
        "suppress-progress": {
          "name": "suppress-progress",
          "type": "boolean",
          "description": "Suppress display of periodic progress dots"
        },
        "target": {
          "name": "target",
          "type": "string",
          "description": "Specify a single resource URN to update. Other resources will not be updated. Multiple resources can be specified using --target urn1 --target urn2",
          "repeatable": true
        },
        "target-dependents": {
          "name": "target-dependents",
          "type": "boolean",
          "description": "Allow updating of dependent targets discovered but not specified in --target list"
        },
        "target-replace": {
          "name": "target-replace",
          "type": "string",
          "description": "Specify a single resource URN to replace. Other resources will not be updated. Shorthand for --target urn --replace urn.",
          "repeatable": true
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "Show a preview of updates to a stack's resources.\n\nThis command displays a preview of the updates to an existing stack whose state is\nrepresented by an existing state file. The new desired state is computed by running\na Pulumi program, and extracting all resource allocations from its resulting object graph.\nThese allocations are then compared against the existing state to determine what\noperations must take place to achieve the desired state. No changes to the stack will\nactually take place.\n\nThe program to run is loaded from the project in the current directory. Use the `-C` or\n`--cwd` flag to use a different directory."
    },
    "project": {
      "type": "menu",
      "executable": false,
      "commands": {
        "ls": {
          "type": "command",
          "flags": {
            "json": {
              "name": "json",
              "type": "boolean",
              "description": "Emit output as JSON"
            },
            "organization": {
              "name": "organization",
              "type": "string",
              "description": "The organization whose projects to list"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "List your Pulumi projects.\n\nThis command lists all Pulumi projects accessible to the current user."
        }
      }
    },
    "refresh": {
      "type": "command",
      "flags": {
        "clear-pending-creates": {
          "name": "clear-pending-creates",
          "type": "boolean",
          "description": "Clear all pending creates, dropping them from the state"
        },
        "client": {
          "name": "client",
          "type": "string",
          "description": "The address of an existing language runtime host to connect to"
        },
        "config": {
          "name": "config",
          "type": "string",
          "description": "Config to use during the refresh and save to the stack config file",
          "repeatable": true
        },
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "config-path": {
          "name": "config-path",
          "type": "boolean",
          "description": "Config keys contain a path to a property in a map or list to set"
        },
        "copilot": {
          "name": "copilot",
          "type": "boolean",
          "description": "[DEPRECATED] Use --neo instead. Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_COPILOT environment variable)"
        },
        "debug": {
          "name": "debug",
          "type": "boolean",
          "description": "Print detailed debugging output during resource operations"
        },
        "diff": {
          "name": "diff",
          "type": "boolean",
          "description": "Display operation as a rich diff showing the overall change"
        },
        "exclude": {
          "name": "exclude",
          "type": "string",
          "description": "Specify a resource URN to ignore. These resources will not be refreshed. Multiple resources can be specified using --exclude urn1 --exclude urn2. Wildcards (*, **) are also supported",
          "repeatable": true
        },
        "exclude-dependents": {
          "name": "exclude-dependents",
          "type": "boolean",
          "description": "Allows ignoring of dependent targets discovered but not specified in --exclude list"
        },
        "exec-agent": {
          "name": "exec-agent",
          "type": "string"
        },
        "exec-kind": {
          "name": "exec-kind",
          "type": "string"
        },
        "expect-no-changes": {
          "name": "expect-no-changes",
          "type": "boolean",
          "description": "Return an error if any changes occur during this refresh. This check happens after the refresh is applied"
        },
        "import-pending-creates": {
          "name": "import-pending-creates",
          "type": "string",
          "description": "A list of form [[URN ID]...] describing the provider IDs of pending creates",
          "repeatable": true
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Serialize the refresh diffs, operations, and overall output as JSON"
        },
        "message": {
          "name": "message",
          "type": "string",
          "description": "Optional message to associate with the update operation"
        },
        "neo": {
          "name": "neo",
          "type": "boolean",
          "description": "Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_NEO environment variable)"
        },
        "parallel": {
          "name": "parallel",
          "type": "int",
          "description": "Allow P resource operations to run in parallel at once (1 for no parallelism)."
        },
        "preview-only": {
          "name": "preview-only",
          "type": "boolean",
          "description": "Only show a preview of the refresh, but don't perform the refresh itself"
        },
        "run-program": {
          "name": "run-program",
          "type": "boolean",
          "description": "Run the program to determine up-to-date state for providers to refresh resources"
        },
        "show-replacement-steps": {
          "name": "show-replacement-steps",
          "type": "boolean",
          "description": "Show detailed resource replacement creates and deletes instead of a single step"
        },
        "show-sames": {
          "name": "show-sames",
          "type": "boolean",
          "description": "Show resources that needn't be updated because they haven't changed, alongside those that do"
        },
        "skip-pending-creates": {
          "name": "skip-pending-creates",
          "type": "boolean",
          "description": "Skip importing pending creates in interactive mode"
        },
        "skip-preview": {
          "name": "skip-preview",
          "type": "boolean",
          "description": "Do not calculate a preview before performing the refresh"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        },
        "suppress-outputs": {
          "name": "suppress-outputs",
          "type": "boolean",
          "description": "Suppress display of stack outputs (in case they contain sensitive values)"
        },
        "suppress-permalink": {
          "name": "suppress-permalink",
          "type": "string",
          "description": "Suppress display of the state permalink"
        },
        "suppress-progress": {
          "name": "suppress-progress",
          "type": "boolean",
          "description": "Suppress display of periodic progress dots"
        },
        "target": {
          "name": "target",
          "type": "string",
          "description": "Specify a single resource URN to refresh. Multiple resource can be specified using: --target urn1 --target urn2",
          "repeatable": true
        },
        "target-dependents": {
          "name": "target-dependents",
          "type": "boolean",
          "description": "Allows updating of dependent targets discovered but not specified in --target list"
        },
        "yes": {
          "name": "yes",
          "type": "boolean",
          "description": "Automatically approve and perform the refresh after previewing it"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "Refresh the resources in a stack.\n\nThis command compares the current stack's resource state with the state known to exist in\nthe actual cloud provider. Any such changes are adopted into the current stack. Note that if\nthe program text isn't updated accordingly, subsequent updates may still appear to be out of\nsync with respect to the cloud provider's source of truth.\n\nThe program to run is loaded from the project in the current directory. Use the `-C` or\n`--cwd` flag to use a different directory."
    },
    "replay-events": {
      "type": "command",
      "flags": {
        "debug": {
          "name": "debug",
          "type": "boolean",
          "description": "Print detailed debugging output during resource operations"
        },
        "delay": {
          "name": "delay",
          "type": "string",
          "description": "Delay display by the given duration. Useful for attaching a debugger."
        },
        "diff": {
          "name": "diff",
          "type": "boolean",
          "description": "Display operation as a rich diff showing the overall change"
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Serialize the preview diffs, operations, and overall output as JSON"
        },
        "period": {
          "name": "period",
          "type": "string",
          "description": "Delay each event by the given duration."
        },
        "preview": {
          "name": "preview",
          "type": "boolean",
          "description": "Must be set for events from a `pulumi preview`."
        },
        "show-config": {
          "name": "show-config",
          "type": "boolean",
          "description": "Show configuration keys and variables"
        },
        "show-reads": {
          "name": "show-reads",
          "type": "boolean",
          "description": "Show resources that are being read in, alongside those being managed directly in the stack"
        },
        "show-replacement-steps": {
          "name": "show-replacement-steps",
          "type": "boolean",
          "description": "Show detailed resource replacement creates and deletes instead of a single step"
        },
        "show-sames": {
          "name": "show-sames",
          "type": "boolean",
          "description": "Show resources that needn't be updated because they haven't changed, alongside those that do"
        },
        "suppress-outputs": {
          "name": "suppress-outputs",
          "type": "boolean",
          "description": "Suppress display of stack outputs (in case they contain sensitive values)"
        },
        "suppress-progress": {
          "name": "suppress-progress",
          "type": "boolean",
          "description": "Suppress display of periodic progress dots"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "kind"
          },
          {
            "name": "events-file"
          }
        ],
        "requiredArguments": 2
      },
      "description": "Replay events from a prior update, refresh, or destroy.\n\nThis command is used to replay events emitted by a prior\ninvocation of the Pulumi CLI (e.g. `pulumi up --event-log [file]`).\n\nThis command loads events from the indicated file and renders them\nusing either the progress view or the diff view.\n\nThe \u003ckind\u003e argument must be one of: update, refresh, destroy, import."
    },
    "schema": {
      "type": "menu",
      "executable": false,
      "commands": {
        "check": {
          "type": "command",
          "flags": {
            "allow-dangling-references": {
              "name": "allow-dangling-references",
              "type": "boolean",
              "description": "Whether references to nonexistent types should be considered errors"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "file"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Check a Pulumi package schema for errors.\n\nEnsure that a Pulumi package schema meets the requirements imposed by the\nschema spec as well as additional requirements imposed by the supported\ntarget languages."
        }
      }
    },
    "stack": {
      "type": "menu",
      "executable": true,
      "flags": {
        "show-ids": {
          "name": "show-ids",
          "type": "boolean",
          "description": "Display each resource's provider-assigned unique ID"
        },
        "show-name": {
          "name": "show-name",
          "type": "boolean",
          "description": "Display only the stack name"
        },
        "show-secrets": {
          "name": "show-secrets",
          "type": "boolean",
          "description": "Display stack outputs which are marked as secret in plaintext"
        },
        "show-urns": {
          "name": "show-urns",
          "type": "boolean",
          "description": "Display each resource's Pulumi-assigned globally unique URN"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        }
      },
      "commands": {
        "change-secrets-provider": {
          "type": "command",
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "new-secrets-provider"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Change the secrets provider for a stack. Valid secret providers types are `default`, `passphrase`, `awskms`, `azurekeyvault`, `gcpkms`, `hashivault`.\n\nTo change to using the Pulumi Default Secrets Provider, use the following:\n\npulumi stack change-secrets-provider default\n\nTo change the stack to use a cloud secrets backend, use one of the following:\n\n* `pulumi stack change-secrets-provider \"awskms://alias/ExampleAlias?region=us-east-1\"`\n* `pulumi stack change-secrets-provider \"awskms://1234abcd-12ab-34cd-56ef-1234567890ab?region=us-east-1\"`\n* `pulumi stack change-secrets-provider \"azurekeyvault://mykeyvaultname.vault.azure.net/keys/mykeyname\"`\n* `pulumi stack change-secrets-provider \"gcpkms://projects/\u003cp\u003e/locations/\u003cl\u003e/keyRings/\u003cr\u003e/cryptoKeys/\u003ck\u003e\"`\n* `pulumi stack change-secrets-provider \"hashivault://mykey\"`"
        },
        "export": {
          "type": "command",
          "flags": {
            "file": {
              "name": "file",
              "type": "string",
              "description": "A filename to write stack output to"
            },
            "show-secrets": {
              "name": "show-secrets",
              "type": "boolean",
              "description": "Emit secrets in plaintext in exported stack. Defaults to `false`"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "version": {
              "name": "version",
              "type": "string",
              "description": "Previous stack version to export. (If unset, will export the latest.)"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "Export a stack's deployment to standard out.\n\nThe deployment can then be hand-edited and used to update the stack via\n`pulumi stack import`. This process may be used to correct inconsistencies\nin a stack's state due to failed deployments, manual changes to cloud\nresources, etc."
        },
        "graph": {
          "type": "command",
          "flags": {
            "dependency-edge-color": {
              "name": "dependency-edge-color",
              "type": "string",
              "description": "Sets the color of dependency edges in the graph"
            },
            "dot-fragment": {
              "name": "dot-fragment",
              "type": "string",
              "description": "An optional DOT fragment that will be inserted at the top of the digraph element. This can be used for styling the graph elements, setting graph properties etc."
            },
            "ignore-dependency-edges": {
              "name": "ignore-dependency-edges",
              "type": "boolean",
              "description": "Ignores edges introduced by dependency resource relationships"
            },
            "ignore-parent-edges": {
              "name": "ignore-parent-edges",
              "type": "boolean",
              "description": "Ignores edges introduced by parent/child resource relationships"
            },
            "parent-edge-color": {
              "name": "parent-edge-color",
              "type": "string",
              "description": "Sets the color of parent edges in the graph"
            },
            "short-node-name": {
              "name": "short-node-name",
              "type": "boolean",
              "description": "Sets the resource name as the node label for each node of the graph"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "filename"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Export a stack's dependency graph to a file.\n\nThis command can be used to view the dependency graph that a Pulumi program\nemitted when it was run. This graph is output in the DOT format. This command operates\non your stack's most recent deployment."
        },
        "history": {
          "type": "command",
          "flags": {
            "full-dates": {
              "name": "full-dates",
              "type": "boolean",
              "description": "Show full dates, instead of relative dates"
            },
            "json": {
              "name": "json",
              "type": "boolean",
              "description": "Emit output as JSON"
            },
            "page": {
              "name": "page",
              "type": "int",
              "description": "Used with 'page-size' to paginate results"
            },
            "page-size": {
              "name": "page-size",
              "type": "int",
              "description": "Used with 'page' to control number of results returned"
            },
            "show-secrets": {
              "name": "show-secrets",
              "type": "boolean",
              "description": "Show secret values when listing config instead of displaying blinded values"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "Choose a stack other than the currently selected one"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "Display history for a stack\n\nThis command displays data about previous updates for a stack."
        },
        "import": {
          "type": "command",
          "flags": {
            "file": {
              "name": "file",
              "type": "string",
              "description": "A filename to read stack input from"
            },
            "force": {
              "name": "force",
              "type": "boolean",
              "description": "Force the import to occur, even if apparent errors are discovered beforehand (not recommended)"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "Import a deployment from standard in into an existing stack.\n\nA deployment that was exported from a stack using `pulumi stack export` and\nhand-edited to correct inconsistencies due to failed updates, manual changes\nto cloud resources, etc. can be reimported to the stack using this command.\nThe updated deployment will be read from standard in."
        },
        "init": {
          "type": "command",
          "flags": {
            "copy-config-from": {
              "name": "copy-config-from",
              "type": "string",
              "description": "The name of the stack to copy existing config from"
            },
            "no-select": {
              "name": "no-select",
              "type": "boolean",
              "description": "Do not select the stack"
            },
            "remote-config": {
              "name": "remote-config",
              "type": "boolean",
              "description": "Store stack configuration remotely"
            },
            "secrets-provider": {
              "name": "secrets-provider",
              "type": "string",
              "description": "The type of the provider that should be used to encrypt and decrypt secrets\n(possible choices: default, passphrase, awskms, azurekeyvault, gcpkms, hashivault)"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to create"
            },
            "teams": {
              "name": "teams",
              "type": "string",
              "description": "A list of team names that should have permission to read and update this stack, once created",
              "repeatable": true
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "stack-name",
                "usage": "[org-name/]\u003cstack-name\u003e"
              }
            ]
          },
          "description": "Create an empty stack with the given name, ready for updates\n\nThis command creates an empty stack with the given name.  It has no resources,\nbut afterwards it can become the target of a deployment using the `update` command.\n\nTo create a stack in an organization when logged in to the Pulumi Cloud,\nprefix the stack name with the organization name and a slash (e.g. 'acmecorp/dev')\n\nBy default, a stack created using the pulumi.com backend will use the pulumi.com secrets\nprovider and a stack created using the local or cloud object storage backend will use the\n`passphrase` secrets provider.  A different secrets provider can be selected by passing the\n`--secrets-provider` flag.\n\nTo use the `passphrase` secrets provider with the pulumi.com backend, use:\n\n* `pulumi stack init --secrets-provider=passphrase`\n\nTo use a cloud secrets provider with any backend, use one of the following:\n\n* `pulumi stack init --secrets-provider=\"awskms://alias/ExampleAlias?region=us-east-1\"`\n* `pulumi stack init --secrets-provider=\"awskms://1234abcd-12ab-34cd-56ef-1234567890ab?region=us-east-1\"`\n* `pulumi stack init --secrets-provider=\"azurekeyvault://mykeyvaultname.vault.azure.net/keys/mykeyname\"`\n* `pulumi stack init --secrets-provider=\"gcpkms://projects/\u003cp\u003e/locations/\u003cl\u003e/keyRings/\u003cr\u003e/cryptoKeys/\u003ck\u003e\"`\n* `pulumi stack init --secrets-provider=\"hashivault://mykey\"`\n\nA stack can be created based on the configuration of an existing stack by passing the\n`--copy-config-from` flag:\n\n* `pulumi stack init --copy-config-from dev`"
        },
        "ls": {
          "type": "command",
          "flags": {
            "all": {
              "name": "all",
              "type": "boolean",
              "description": "List all stacks instead of just stacks for the current project"
            },
            "json": {
              "name": "json",
              "type": "boolean",
              "description": "Emit output as JSON"
            },
            "organization": {
              "name": "organization",
              "type": "string",
              "description": "Filter returned stacks to those in a specific organization"
            },
            "project": {
              "name": "project",
              "type": "string",
              "description": "Filter returned stacks to those with a specific project name"
            },
            "tag": {
              "name": "tag",
              "type": "string",
              "description": "Filter returned stacks to those in a specific tag (tag-name or tag-name=tag-value)"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "List stacks\n\nThis command lists stacks. By default only stacks with the same project name as the\ncurrent workspace will be returned. By passing --all, all stacks you have access to\nwill be listed.\n\nResults may be further filtered by passing additional flags. Tag filters may include\nthe tag name as well as the tag value, separated by an equals sign. For example\n'environment=production' or just 'gcp:project'."
        },
        "output": {
          "type": "command",
          "flags": {
            "json": {
              "name": "json",
              "type": "boolean",
              "description": "Emit output as JSON"
            },
            "shell": {
              "name": "shell",
              "type": "boolean",
              "description": "Emit output as a shell script"
            },
            "show-secrets": {
              "name": "show-secrets",
              "type": "boolean",
              "description": "Display outputs which are marked as secret in plaintext"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "property-name"
              }
            ]
          },
          "description": "Show a stack's output properties.\n\nBy default, this command lists all output properties exported from a stack.\nIf a specific property-name is supplied, just that property's value is shown."
        },
        "rename": {
          "type": "command",
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "new-stack-name"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Rename an existing stack.\n\nNote: Because renaming a stack will change the value of `getStack()` inside a Pulumi program, if this\nname is used as part of a resource's name, the next `pulumi up` will want to delete the old resource and\ncreate a new copy. For now, if you don't want these changes to be applied, you should rename your stack\nback to its previous name.\nYou can also rename the stack's project by passing a fully-qualified stack name as well. For example:\n'robot-co/new-project-name/production'. However in order to update the stack again, you would also need\nto update the name field of Pulumi.yaml, so the project names match."
        },
        "rm": {
          "type": "command",
          "flags": {
            "force": {
              "name": "force",
              "type": "boolean",
              "description": "Forces deletion of the stack, leaving behind any resources managed by the stack"
            },
            "preserve-config": {
              "name": "preserve-config",
              "type": "boolean",
              "description": "Do not delete the corresponding Pulumi.\u003cstack-name\u003e.yaml configuration file for the stack"
            },
            "remove-backups": {
              "name": "remove-backups",
              "type": "boolean",
              "description": "Additionally remove backups of the stack, if using the DIY backend"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts, and proceed with removal anyway"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "stack-name"
              }
            ]
          },
          "description": "Remove a stack and its configuration\n\nThis command removes a stack and its configuration state.  Please refer to the\n`destroy` command for removing a resources, as this is a distinct operation.\n\nAfter this command completes, the stack will no longer be available for updates."
        },
        "select": {
          "type": "command",
          "flags": {
            "create": {
              "name": "create",
              "type": "boolean",
              "description": "If selected stack does not exist, create it"
            },
            "secrets-provider": {
              "name": "secrets-provider",
              "type": "string",
              "description": "Use with --create flag, The type of the provider that should be used to encrypt and decrypt secrets\n(possible choices: default, passphrase, awskms, azurekeyvault, gcpkms, hashivault)"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to select"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "stack"
              }
            ]
          },
          "description": "Switch the current workspace to the given stack.\n\nSelecting a stack allows you to use commands like `config`, `preview`, and `update`\nwithout needing to type the stack name each time.\n\nIf no \u003cstack\u003e argument is supplied, you will be prompted to select one interactively.\nIf provided stack name is not found you may pass the --create flag to create and select it"
        },
        "tag": {
          "type": "menu",
          "executable": false,
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "commands": {
            "get": {
              "type": "command",
              "arguments": {
                "arguments": [
                  {
                    "name": "name"
                  }
                ],
                "requiredArguments": 1
              },
              "description": "Get a single stack tag value"
            },
            "ls": {
              "type": "command",
              "flags": {
                "json": {
                  "name": "json",
                  "type": "boolean",
                  "description": "Emit output as JSON"
                }
              },
              "arguments": {
                "arguments": []
              },
              "description": "List all stack tags"
            },
            "rm": {
              "type": "command",
              "arguments": {
                "arguments": [
                  {
                    "name": "name"
                  }
                ],
                "requiredArguments": 1
              },
              "description": "Remove a stack tag"
            },
            "set": {
              "type": "command",
              "arguments": {
                "arguments": [
                  {
                    "name": "name"
                  },
                  {
                    "name": "value"
                  }
                ],
                "requiredArguments": 2
              },
              "description": "Set a stack tag"
            }
          }
        },
        "unselect": {
          "type": "command",
          "arguments": {
            "arguments": []
          },
          "description": "Resets stack selection from the current workspace.\n\nThis way, next time pulumi needs to execute an operation, the user is prompted with one of the stacks to select\nfrom."
        }
      }
    },
    "state": {
      "type": "menu",
      "executable": false,
      "commands": {
        "delete": {
          "type": "command",
          "flags": {
            "all": {
              "name": "all",
              "type": "boolean",
              "description": "Delete all resources in the stack"
            },
            "force": {
              "name": "force",
              "type": "boolean",
              "description": "Force deletion of protected resources"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "target-dependents": {
              "name": "target-dependents",
              "type": "boolean",
              "description": "Delete the URN and all its dependents"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "resource-urn"
              }
            ]
          },
          "description": "Deletes a resource from a stack's state\n\nThis command deletes a resource from a stack's state, as long as it is safe to do so. The resource is specified\nby its Pulumi URN. If the URN is omitted, this command will prompt for it.\n\nResources can't be deleted if other resources depend on it or are parented to it. Protected resources\nwill not be deleted unless specifically requested using the --force flag.\n\nMake sure that URNs are single-quoted to avoid having characters unexpectedly interpreted by the shell.\n\nTo see the list of URNs in a stack, use `pulumi stack --show-urns`."
        },
        "edit": {
          "type": "command",
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "[EXPERIMENTAL] Edit the current stack's state in your EDITOR\n\nThis command can be used to surgically edit a stack's state in the editor\nspecified by the EDITOR environment variable and will provide the user with\na preview showing a diff of the altered state."
        },
        "move": {
          "type": "command",
          "flags": {
            "dest": {
              "name": "dest",
              "type": "string",
              "description": "The name of the stack to move resources to"
            },
            "include-parents": {
              "name": "include-parents",
              "type": "boolean",
              "description": "Include all the parents of the moved resources as well"
            },
            "source": {
              "name": "source",
              "type": "string",
              "description": "The name of the stack to move resources from"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Automatically approve and perform the move"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "urn"
              }
            ],
            "requiredArguments": 1,
            "variadic": true
          },
          "description": "Move resources from one stack to another\n\nThis command can be used to move resources from one stack to another. This can be useful when\nsplitting a stack into multiple stacks or when merging multiple stacks into one."
        },
        "protect": {
          "type": "command",
          "flags": {
            "all": {
              "name": "all",
              "type": "boolean",
              "description": "Protect all resources in the checkpoint"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "resource-urn"
              }
            ],
            "variadic": true
          },
          "description": "Protect resource in a stack's state\n\nThis command sets the 'protect' bit on one or more resources, preventing those resources from being deleted.\n\nCaution: this command is a low-level operation that directly modifies your stack's state.\nSetting the 'protect' bit on a resource in your stack's state is not sufficient to protect it in\nall cases. If your program does not also set the 'protect' resource option, Pulumi will\nunprotect the resource the next time your program runs (e.g. as part of a `pulumi up`).\n\nSee https://www.pulumi.com/docs/iac/concepts/options/protect/ for more information on\nthe 'protect' resource option and how it can be used to protect resources in your program.\n\nTo unprotect a resource, use `pulumi unprotect`on the resource URN.\n\nTo see the list of URNs in a stack, use `pulumi stack --show-urns`."
        },
        "rename": {
          "type": "command",
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "resource-urn"
              },
              {
                "name": "new-name"
              }
            ]
          },
          "description": "Renames a resource from a stack's state\n\nThis command renames a resource from a stack's state. The resource is specified\nby its Pulumi URN and the new name of the resource.\n\nMake sure that URNs are single-quoted to avoid having characters unexpectedly interpreted by the shell.\n\nTo see the list of URNs in a stack, use `pulumi stack --show-urns`."
        },
        "repair": {
          "type": "command",
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Automatically approve and perform the repair"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "Repair an invalid state,\n\nThis command can be used to repair an invalid state file. It will attempt to\nsort resources that appear out of order and remove references to resources that\nare no longer present in the state. If the state is already valid, this command\nwill not attempt to make or write any changes. If the state is not already\nvalid, and remains invalid after repair has been attempted, this command will\nnot write any changes."
        },
        "taint": {
          "type": "command",
          "flags": {
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "resource-urn"
              }
            ],
            "variadic": true
          },
          "description": "Taint one or more resources in the stack's state.\n\nThis has the effect of ensuring the resources are destroyed and recreated upon the next `pulumi up`.\n\nTo see the list of URNs in a stack, use `pulumi stack --show-urns`."
        },
        "unprotect": {
          "type": "command",
          "flags": {
            "all": {
              "name": "all",
              "type": "boolean",
              "description": "Unprotect all resources in the checkpoint"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "resource-urn"
              }
            ],
            "variadic": true
          },
          "description": "Unprotect resources in a stack's state\n\nThis command clears the 'protect' bit on one or more resources, allowing those resources to be deleted.\n\nTo see the list of URNs in a stack, use `pulumi stack --show-urns`."
        },
        "untaint": {
          "type": "command",
          "flags": {
            "all": {
              "name": "all",
              "type": "boolean",
              "description": "Untaint all resources in the checkpoint"
            },
            "stack": {
              "name": "stack",
              "type": "string",
              "description": "The name of the stack to operate on. Defaults to the current stack"
            },
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Skip confirmation prompts"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "resource-urn"
              }
            ],
            "variadic": true
          },
          "description": "Untaint one or more resources in the stack's state.\n\nAfter running this, the resources will no longer be destroyed and recreated upon the next `pulumi up`.\n\nTo see the list of URNs in a stack, use `pulumi stack --show-urns`."
        },
        "upgrade": {
          "type": "command",
          "flags": {
            "yes": {
              "name": "yes",
              "type": "boolean",
              "description": "Automatically approve and perform the upgrade"
            }
          },
          "arguments": {
            "arguments": []
          },
          "description": "Migrates the current backend to the latest supported version\n\nThis only has an effect on DIY backends."
        }
      }
    },
    "template": {
      "type": "menu",
      "executable": false,
      "commands": {
        "publish": {
          "type": "command",
          "flags": {
            "name": {
              "name": "name",
              "type": "string",
              "description": "The name of the template (required)"
            },
            "publisher": {
              "name": "publisher",
              "type": "string",
              "description": "The publisher of the template (e.g., 'pulumi'). Defaults to the default organization in your pulumi config."
            },
            "version": {
              "name": "version",
              "type": "string",
              "description": "The version of the template (required, semver format)"
            }
          },
          "arguments": {
            "arguments": [
              {
                "name": "directory"
              }
            ],
            "requiredArguments": 1
          },
          "description": "Publish a template to the Private Registry.\n\nThis command publishes a template directory to the Private Registry."
        }
      }
    },
    "up": {
      "type": "command",
      "flags": {
        "attach-debugger": {
          "name": "attach-debugger",
          "type": "string",
          "description": "Enable the ability to attach a debugger to the program and source based plugins being executed. Can limit debug type to 'program', 'plugins', 'plugin:\u003cname\u003e' or 'all'.",
          "repeatable": true
        },
        "client": {
          "name": "client",
          "type": "string",
          "description": "The address of an existing language runtime host to connect to"
        },
        "config": {
          "name": "config",
          "type": "string",
          "description": "Config to use during the update and save to the stack config file",
          "repeatable": true
        },
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "config-path": {
          "name": "config-path",
          "type": "boolean",
          "description": "Config keys contain a path to a property in a map or list to set"
        },
        "continue-on-error": {
          "name": "continue-on-error",
          "type": "boolean",
          "description": "Continue updating resources even if an error is encountered (can also be set with PULUMI_CONTINUE_ON_ERROR environment variable)"
        },
        "copilot": {
          "name": "copilot",
          "type": "boolean",
          "description": "[DEPRECATED] Use --neo instead. Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_COPILOT environment variable)"
        },
        "debug": {
          "name": "debug",
          "type": "boolean",
          "description": "Print detailed debugging output during resource operations"
        },
        "diff": {
          "name": "diff",
          "type": "boolean",
          "description": "Display operation as a rich diff showing the overall change"
        },
        "exclude": {
          "name": "exclude",
          "type": "string",
          "description": "Specify a resource URN to ignore. These resources will not be updated. Multiple resources can be specified using --exclude urn1 --exclude urn2. Wildcards (*, **) are also supported",
          "repeatable": true
        },
        "exclude-dependents": {
          "name": "exclude-dependents",
          "type": "boolean",
          "description": "Allows ignoring of dependent targets discovered but not specified in --exclude list"
        },
        "exec-agent": {
          "name": "exec-agent",
          "type": "string"
        },
        "exec-kind": {
          "name": "exec-kind",
          "type": "string"
        },
        "expect-no-changes": {
          "name": "expect-no-changes",
          "type": "boolean",
          "description": "Return an error if any changes occur during this update. This check happens after the update is applied"
        },
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Serialize the update diffs, operations, and overall output as JSON"
        },
        "message": {
          "name": "message",
          "type": "string",
          "description": "Optional message to associate with the update operation"
        },
        "neo": {
          "name": "neo",
          "type": "boolean",
          "description": "Enable Pulumi Neo's assistance for improved CLI experience and insights (can also be set with PULUMI_NEO environment variable)"
        },
        "parallel": {
          "name": "parallel",
          "type": "int",
          "description": "Allow P resource operations to run in parallel at once (1 for no parallelism)."
        },
        "plan": {
          "name": "plan",
          "type": "string",
          "description": "[EXPERIMENTAL] Path to a plan file to use for the update. The update will not perform operations that exceed its plan (e.g. replacements instead of updates, or updates insteadof sames)."
        },
        "policy-pack": {
          "name": "policy-pack",
          "type": "string",
          "description": "Run one or more policy packs as part of this update",
          "repeatable": true
        },
        "policy-pack-config": {
          "name": "policy-pack-config",
          "type": "string",
          "description": "Path to JSON file containing the config for the policy pack of the corresponding \"--policy-pack\" flag",
          "repeatable": true
        },
        "refresh": {
          "name": "refresh",
          "type": "string",
          "description": "Refresh the state of the stack's resources before this update"
        },
        "replace": {
          "name": "replace",
          "type": "string",
          "description": "Specify a single resource URN to replace. Multiple resources can be specified using --replace urn1 --replace urn2. Wildcards (*, **) are also supported",
          "repeatable": true
        },
        "run-program": {
          "name": "run-program",
          "type": "boolean",
          "description": "Run the program to determine up-to-date state for providers to refresh resources, this only applies if --refresh is set"
        },
        "secrets-provider": {
          "name": "secrets-provider",
          "type": "string",
          "description": "The type of the provider that should be used to encrypt and decrypt secrets (possible choices: default, passphrase, awskms, azurekeyvault, gcpkms, hashivault). Only used when creating a new stack from an existing template"
        },
        "show-config": {
          "name": "show-config",
          "type": "boolean",
          "description": "Show configuration keys and variables"
        },
        "show-full-output": {
          "name": "show-full-output",
          "type": "boolean",
          "description": "Display full length of inputs \u0026 outputs"
        },
        "show-policy-remediations": {
          "name": "show-policy-remediations",
          "type": "boolean",
          "description": "Show per-resource policy remediation details instead of a summary"
        },
        "show-reads": {
          "name": "show-reads",
          "type": "boolean",
          "description": "Show resources that are being read in, alongside those being managed directly in the stack"
        },
        "show-replacement-steps": {
          "name": "show-replacement-steps",
          "type": "boolean",
          "description": "Show detailed resource replacement creates and deletes instead of a single step"
        },
        "show-sames": {
          "name": "show-sames",
          "type": "boolean",
          "description": "Show resources that don't need be updated because they haven't changed, alongside those that do"
        },
        "show-secrets": {
          "name": "show-secrets",
          "type": "boolean",
          "description": "Show secret outputs in the CLI output"
        },
        "skip-preview": {
          "name": "skip-preview",
          "type": "boolean",
          "description": "Do not calculate a preview before performing the update"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        },
        "strict": {
          "name": "strict",
          "type": "boolean",
          "description": "[EXPERIMENTAL] Enable strict plan behavior: generate a plan during preview and constrain the update to that plan (opt-in). Cannot be used with --skip-preview."
        },
        "suppress-outputs": {
          "name": "suppress-outputs",
          "type": "boolean",
          "description": "Suppress display of stack outputs (in case they contain sensitive values)"
        },
        "suppress-permalink": {
          "name": "suppress-permalink",
          "type": "string",
          "description": "Suppress display of the state permalink"
        },
        "suppress-progress": {
          "name": "suppress-progress",
          "type": "boolean",
          "description": "Suppress display of periodic progress dots"
        },
        "target": {
          "name": "target",
          "type": "string",
          "description": "Specify a single resource URN to update. Other resources will not be updated. Multiple resources can be specified using --target urn1 --target urn2. Wildcards (*, **) are also supported",
          "repeatable": true
        },
        "target-dependents": {
          "name": "target-dependents",
          "type": "boolean",
          "description": "Allows updating of dependent targets discovered but not specified in --target list"
        },
        "target-replace": {
          "name": "target-replace",
          "type": "string",
          "description": "Specify a single resource URN to replace. Other resources will not be updated. Shorthand for --target urn --replace urn.",
          "repeatable": true
        },
        "yes": {
          "name": "yes",
          "type": "boolean",
          "description": "Automatically approve and perform the update after previewing it"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "template-or-url",
            "usage": "[template|url]"
          }
        ]
      },
      "description": "Create or update the resources in a stack.\n\nThis command creates or updates resources in a stack. The new desired goal state for the target stack\nis computed by running the current Pulumi program and observing all resource allocations to produce a\nresource graph. This goal state is then compared against the existing state to determine what create,\nread, update, and/or delete operations must take place to achieve the desired goal state, in the most\nminimally disruptive way. This command records a full transactional snapshot of the stack's new state\nafterwards so that the stack may be updated incrementally again later on.\n\nThe program to run is loaded from the project in the current directory by default. Use the `-C` or\n`--cwd` flag to use a different directory.\n\nNote: An optional template name or URL can be provided to deploy from a template. When used, a temporary\n project is created, deployed, and then deleted, leaving only the stack state."
    },
    "version": {
      "type": "command",
      "arguments": {
        "arguments": []
      },
      "description": "Print Pulumi's version number"
    },
    "view-trace": {
      "type": "command",
      "flags": {
        "port": {
          "name": "port",
          "type": "int",
          "description": "the port the trace viewer will listen on"
        }
      },
      "arguments": {
        "arguments": [
          {
            "name": "trace-file"
          }
        ],
        "requiredArguments": 1
      },
      "description": "Display a trace from the Pulumi CLI.\n\nThis command is used to display execution traces collected by a prior\ninvocation of the Pulumi CLI.\n\nThis command loads trace data from the indicated file and starts a\nwebserver to display the trace. By default, this server will listen\nport 8008; the --port flag can be used to change this if necessary."
    },
    "watch": {
      "type": "command",
      "flags": {
        "config": {
          "name": "config",
          "type": "string",
          "description": "Config to use during the update",
          "repeatable": true
        },
        "config-file": {
          "name": "config-file",
          "type": "string",
          "description": "Use the configuration values in the specified file rather than detecting the file name"
        },
        "config-path": {
          "name": "config-path",
          "type": "boolean",
          "description": "Config keys contain a path to a property in a map or list to set"
        },
        "debug": {
          "name": "debug",
          "type": "boolean",
          "description": "Print detailed debugging output during resource operations"
        },
        "exec-kind": {
          "name": "exec-kind",
          "type": "string"
        },
        "message": {
          "name": "message",
          "type": "string",
          "description": "Optional message to associate with each update operation"
        },
        "parallel": {
          "name": "parallel",
          "type": "int",
          "description": "Allow P resource operations to run in parallel at once (1 for no parallelism)."
        },
        "path": {
          "name": "path",
          "type": "string",
          "description": "Specify one or more relative or absolute paths that need to be watched. A path can point to a folder or a file. Defaults to working directory",
          "repeatable": true
        },
        "policy-pack": {
          "name": "policy-pack",
          "type": "string",
          "description": "Run one or more policy packs as part of each update",
          "repeatable": true
        },
        "policy-pack-config": {
          "name": "policy-pack-config",
          "type": "string",
          "description": "Path to JSON file containing the config for the policy pack of the corresponding \"--policy-pack\" flag",
          "repeatable": true
        },
        "refresh": {
          "name": "refresh",
          "type": "boolean",
          "description": "Refresh the state of the stack's resources before each update"
        },
        "secrets-provider": {
          "name": "secrets-provider",
          "type": "string",
          "description": "The type of the provider that should be used to encrypt and decrypt secrets (possible choices: default, passphrase, awskms, azurekeyvault, gcpkms, hashivault). Only used when creating a new stack from an existing template"
        },
        "show-config": {
          "name": "show-config",
          "type": "boolean",
          "description": "Show configuration keys and variables"
        },
        "show-replacement-steps": {
          "name": "show-replacement-steps",
          "type": "boolean",
          "description": "Show detailed resource replacement creates and deletes instead of a single step"
        },
        "show-sames": {
          "name": "show-sames",
          "type": "boolean",
          "description": "Show resources that don't need be updated because they haven't changed, alongside those that do"
        },
        "stack": {
          "name": "stack",
          "type": "string",
          "description": "The name of the stack to operate on. Defaults to the current stack"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "[EXPERIMENTAL] Continuously update the resources in a stack.\n\nThis command watches the working directory or specified paths for the current project and updates\nthe active stack whenever the project changes.  In parallel, logs are collected for all resources\nin the stack and displayed along with update progress.\n\nThe program to watch is loaded from the project in the current directory by default. Use the `-C` or\n`--cwd` flag to use a different directory."
    },
    "whoami": {
      "type": "command",
      "flags": {
        "json": {
          "name": "json",
          "type": "boolean",
          "description": "Emit output as JSON"
        },
        "verbose": {
          "name": "verbose",
          "type": "boolean",
          "description": "Print detailed whoami information"
        }
      },
      "arguments": {
        "arguments": []
      },
      "description": "Display the current logged-in user\n\nDisplays the username of the currently logged in user."
    }
  }
}
