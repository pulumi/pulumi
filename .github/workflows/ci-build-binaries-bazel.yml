name: Build Binaries (Bazel)

permissions:
  contents: read

on:
  workflow_call:
    inputs:
      ref:
        required: true
        description: "GitHub ref to use"
        type: string
      version:
        required: true
        description: "Version to produce"
        type: string
      artifact-suffix:
        required: false
        description: "Suffix to append to artifact names"
        type: string

defaults:
  run:
    shell: bash

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: All platforms
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}

      - name: Mount bazel cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/bazel
          key: bazel-build-binaries-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'go.work.sum') }}
          restore-keys: |
            bazel-build-binaries-${{ runner.os }}-

      - name: Build all release archives
        env:
          PULUMI_VERSION: ${{ inputs.version }}
        run: |
          bazel build //build/release:all_archives \
            --stamp \
            --define PULUMI_VERSION="${PULUMI_VERSION}"

      - name: Copy and rename archives to output directory
        env:
          PULUMI_VERSION: ${{ inputs.version }}
        run: |
          mkdir -p dist
          # Rename from default Bazel output names to versioned release names.
          cp bazel-bin/build/release/archive_linux_amd64.tar.gz   "dist/pulumi-v${PULUMI_VERSION}-linux-x64.tar.gz"
          cp bazel-bin/build/release/archive_linux_arm64.tar.gz   "dist/pulumi-v${PULUMI_VERSION}-linux-arm64.tar.gz"
          cp bazel-bin/build/release/archive_darwin_amd64.tar.gz  "dist/pulumi-v${PULUMI_VERSION}-darwin-x64.tar.gz"
          cp bazel-bin/build/release/archive_darwin_arm64.tar.gz  "dist/pulumi-v${PULUMI_VERSION}-darwin-arm64.tar.gz"
          cp bazel-bin/build/release/archive_windows_amd64.zip    "dist/pulumi-v${PULUMI_VERSION}-windows-x64.zip"
          cp bazel-bin/build/release/archive_windows_arm64.zip    "dist/pulumi-v${PULUMI_VERSION}-windows-arm64.zip"
          ls -la dist/

      # Upload per-platform artifacts matching the naming convention expected
      # by downstream workflows (sign.yml, ci-prepare-release.yml).
      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-cli-linux-amd64${{ inputs.artifact-suffix || '' }}
          retention-days: 7
          path: dist/pulumi-*-linux-x64.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-cli-linux-arm64${{ inputs.artifact-suffix || '' }}
          retention-days: 7
          path: dist/pulumi-*-linux-arm64.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-cli-darwin-amd64${{ inputs.artifact-suffix || '' }}
          retention-days: 7
          path: dist/pulumi-*-darwin-x64.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-cli-darwin-arm64${{ inputs.artifact-suffix || '' }}
          retention-days: 7
          path: dist/pulumi-*-darwin-arm64.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-cli-windows-amd64${{ inputs.artifact-suffix || '' }}
          retention-days: 7
          path: dist/pulumi-*-windows-x64.zip

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-cli-windows-arm64${{ inputs.artifact-suffix || '' }}
          retention-days: 7
          path: dist/pulumi-*-windows-arm64.zip
